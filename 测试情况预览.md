# 首页聊天黑盒测试情况预览

## 测试范围

本文只覆盖首页聊天入口，不包含动作库页面、训练编排页、训练计划页、训练执行页的独立页面操作。

黑盒验收重点：

- 用户输入后，AI 是否识别正确意图。
- AI 是否在合适时机触发动作推荐、单次训练编排或长期训练计划卡片。
- AI 是否基于动作库生成结果，不编造不可执行动作。
- 信息不足、候选不足、风险较高或意图不清时，是否追问或解释，而不是错误推送。
- 多轮对话中，系统是否正确继承上下文、修正上下文、处理引用和避免冲突。

## 设计方式

首页聊天的常见使用方式是多轮对话。本文不再把“单轮”和“多轮”拆成两套重复用例，而是每个用例都按 3 轮流程设计：

- 第 1 轮：作为单轮保证，验证首轮意图识别、是否触发卡片、是否正确追问。
- 第 2 轮：验证上下文继承、信息补齐、条件覆盖或流程升级。
- 第 3 轮：验证继续修改、引用、确认、刷新或边界收束。

如果第 1 轮已经失败，则该用例的多轮流程不继续执行，先记录为单轮基础能力失败。

## 三轮流程用例

| ID | 流程目标 | 第 1 轮用户输入 | 第 1 轮期望 | 第 2 轮用户输入 | 第 2 轮期望 | 第 3 轮用户输入 | 第 3 轮期望 |
|---|---|---|---|---|---|---|---|
| F01 | 纯动作推荐到刷新推荐 | 今天我想练胸 | 识别为胸部动作推荐；触发动作推荐卡片；不生成 routine 或 plan。 | 换一批 | 继续沿用胸部目标；刷新动作推荐；优先排除上一张卡片已有动作。 | 推荐几个不用器械的 | 继续沿用胸部目标，同时加入无器械条件；推荐自重胸部动作。 |
| F02 | 动作推荐升级为单次训练 | 推荐几个练腿动作 | 识别为腿部动作推荐；触发动作推荐卡片；不要求先补充时长。 | 给我做成20分钟训练 | 继承腿部目标，触发单次 routine；卡片包含热身、主训练、拉伸。 | 我没有器械 | 基于同一训练目标调整为无器械腿部 routine；不重新追问已知目标和时长。 |
| F03 | 信息不足时逐步补齐 routine | 给我一套训练 | 不直接生成随机卡片；追问目标、时长、器械或场地等关键条件。 | 练胸，20分钟 | 继承“要一套训练”的意图；仍可追问器械/场地，或在候选足够时按默认简单策略生成。 | 没有器械 | 条件补齐后生成胸部 20 分钟无器械 routine；不反复追问已经提供的信息。 |
| F04 | 单次训练条件一次给齐后继续调整 | 今天在家练背30分钟 | 识别为单次 routine；生成居家背部 30 分钟三段式训练。 | 改成45分钟 | 保留背部和居家条件，只调整训练量到约 45 分钟。 | 太难了，降低一点 | 保留原 routine 目标和结构，降低动作难度、组数或强度；不生成无关计划。 |
| F05 | 长期计划逐步补齐 | 给我一个每周训练计划 | 不直接生成空泛计划；追问目标、频率、单次时长或器械条件。 | 每周4练，每次45分钟 | 继承长期计划意图；继续补齐目标或器械条件。 | 增肌，有健身房器械 | 生成每周 4 练、每次约 45 分钟、增肌、健身房条件的长期 plan 卡片。 |
| F06 | 长期计划语义区分 | 给我一个6天训练计划 | 识别为长期计划；6 天应理解为计划周期，允许包含休息/恢复日。 | 改成每周6练 | 将语义切换为周训练频率；不把它当成普通 6 天周期。 | 未来6天每天练 | 将语义切换为明确日历范围；应表达为未来 6 天安排，而不是普通周期模板。 |
| F07 | 当前消息覆盖历史条件 | 我有哑铃 | 记录器械条件；不必生成训练卡片。 | 今天练胸30分钟 | 继承哑铃条件，生成可使用哑铃的胸部 routine。 | 但今天不用器械 | 当前消息覆盖历史器械条件；调整为无器械胸部 routine。 |
| F08 | 临时限制不变成永久偏好 | 今天不想练腿 | 识别为临时上下文；不生成训练卡片或可给出确认式回应。 | 推荐全身训练 | 推荐或生成方案时避免腿部重点；不把“不练腿”当永久偏好。 | 明天可以练腿，给我腿部动作 | 当前消息覆盖临时限制；可以推荐腿部动作。 |
| F12 | 候选不足或目标不清时不编造 | 推荐一个动作 | 不随意推荐；追问想练部位、目标或器械条件。 | 就随便 | 仍不生成随机不可解释推荐；应给出可选方向或继续澄清。 | 那练核心吧 | 目标明确后推荐核心动作；触发动作推荐卡片。 |
| F13 | 非健身话题不触发训练流程 | 明天天气怎么样？ | 识别为非健身问题；不触发训练卡片。 | 那我今天练胸 | 重新进入健身流程，触发胸部动作推荐或按信息充足程度生成训练建议。 | 做成20分钟 | 继承胸部目标，升级为 20 分钟单次 routine。 |
| F14 | 从建议问答进入计划生成 | 你觉得减脂应该怎么练？ | 作为训练建议回答；不默认直接推送计划卡片；可询问是否生成计划。 | 给我安排每周3练 | 继承减脂目标；识别长期计划意图，补齐单次时长和器械条件。 | 每次30分钟，没有器械 | 生成减脂、每周 3 练、每次约 30 分钟、无器械长期 plan。 |
| F15 | 明确引用最近卡片 | 今天想练胸 | 推荐胸部动作卡片。 | 把它变成20分钟训练 | “它”解析为最近推荐卡片；生成胸部 20 分钟 routine。 | 第一个动作怎么做 | 解释最近 routine 或推荐中的第一个动作；不重新推荐一批动作。 |
| F16 | 引用歧义必须澄清 | 推荐胸部动作 | 生成胸部动作推荐卡片。 | 再推荐腿部动作 | 生成腿部动作推荐卡片，当前会话存在多个可引用卡片。 | 把这个变成训练 | 如果“这个”无法唯一定位，应让用户确认胸部还是腿部；不直接选择其中一个生成 routine。 |
| F17 | 局部修改不重生成整套 | 给我一套胸部20分钟无器械训练 | 生成胸部 20 分钟无器械 routine。 | 把俯卧撑换掉 | 只替换目标动作；其他动作、阶段、顺序、休息配置保持不变。 | 再把它调简单点 | 继续基于修改后的 routine 降低难度；不回退到原始版本。 |
| F18 | 重复动作修改需要确认范围 | 给我一套循环胸部训练 | 生成 routine；同一动作可能在循环中重复出现。 | 把俯卧撑换掉 | 如果同一动作出现多次且范围不明确，应询问替换哪一次或是否全部替换。 | 全部换掉 | 只在用户确认后替换所有出现位置，并保留其他未点名内容。 |
| F19 | 计划频率修改保持计划语义 | 给我一个每周4练增肌计划 | 生成长期 plan；不生成单次 routine。 | 改成每周3练 | 修改长期计划频率；仍保持 plan 语义。 | 每次控制在40分钟 | 保留每周 3 练和增肌目标，调整单次训练时长到约 40 分钟。 |
| F20 | 目标切换时当前消息优先 | 今天我想练胸 | 推荐胸部动作或生成胸部相关建议。 | 算了，今天练腿 | 当前消息切换目标；后续推荐应变为腿部，不继续胸部。 | 20分钟，无器械 | 继承新的腿部目标，生成 20 分钟无器械腿部 routine。 |
| F21 | 过多目标和时长冲突 | 今天练胸，再练背，再练腿，15分钟 | 识别训练目标过多且时间不足；不直接生成大容量方案，应压缩或追问优先级。 | 优先练胸 | 收束到胸部目标；可以推荐胸部动作或继续补齐器械条件。 | 没有器械 | 生成或推荐胸部无器械短时方案，容量应匹配 15 分钟。 |
| F22 | 高强度请求需要保守默认 | 给我安排一套专业健美选手训练 | 用户经验未知时不直接生成高风险高容量方案；追问经验或默认降低强度。 | 我是新手 | 明确降级为新手友好方案；不使用专业健美高容量训练。 | 每次30分钟，健身房 | 生成新手、健身房、30 分钟 routine 或 plan，强度受控。 |
| F23 | 点名不存在动作不编造 | 我想练你们库里没有的超级飞鸟跳 | 找不到动作时不编造卡片；引导用户说明训练目标或部位。 | 那练胸吧 | 目标明确后回到胸部推荐流程。 | 不要器械 | 加入无器械条件，推荐自重胸部动作。 |
| F24 | 问动作说明不误触发新卡片 | 推荐几个核心动作 | 推荐核心动作卡片。 | 第一个动作怎么做 | 解释第一个动作的做法、注意点或适用人群；不刷新推荐卡片。 | 太难了，换简单点 | 将第一个动作或当前推荐向低难度替代调整；不重置为无关目标。 |
