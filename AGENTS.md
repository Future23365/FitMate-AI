# AGENTS.md

## 项目概述

本项目是一个 AI 健身聊天助手。系统通过自然语言交互理解用户的健身目标、身体状态、训练限制、训练偏好和可用时间，并据此生成、调整和执行个性化训练计划。

完整架构说明见：`docs/architecture.md`。

## 技术栈

- Next.js App Router
- React
- TypeScript
- Tailwind CSS
- shadcn/ui
- Prisma
- PostgreSQL
- Zod
- OpenAI SDK / Vercel AI SDK / DeepSeek AI SDK

## 架构规则

- 保持 UI 层、API 层、AI 编排层、领域服务层和数据访问层之间的职责分离。
- 不要把业务逻辑直接写在 API Route 中。
- 不要在 UI 组件中直接调用数据库或 AI 服务。
- 确定性的健身业务规则应放在领域服务中。
- AI 编排逻辑应与数据校验、权限控制和持久化逻辑分离。

## AI 规则

- 模型生成的数据必须使用 Structured Outputs、Zod Schema 或 JSON Schema 进行结构约束。
- 所有模型输出在保存或执行前都必须经过服务端校验。
- Tool Calling 必须通过服务端函数执行。
- AI 不能直接写入未经校验的训练计划。
- AI 选择或生成的 exerciseId 必须经过数据库校验。
- 不要向 AI 工具暴露任意 SQL 查询能力。

## 健身领域规则

- LLM 负责理解和生成，后端服务负责规则约束和最终执行。
- 训练计划必须尊重用户的目标、伤病情况、可用器械、训练经验和可用时间。
- 保存到系统中的训练动作必须来自数据库。
- 对新手、疼痛用户或有伤病限制的用户，应避免高风险或高冲击动作。
- 不要提供医疗诊断或治疗建议。

## 数据规则

- PostgreSQL 是系统事实数据来源。
- 使用 Prisma 进行数据库访问。
- 所有用户输入必须使用 Zod 校验。
- 所有 AI 输出必须使用 Zod 或 JSON Schema 校验。
- 所有用户私有数据查询都必须基于 userId 做权限隔离。
- 优先使用结构化数据库过滤，再使用向量检索或语义排序。

## 安全规则

- 不要绕过权限校验。
- 不要信任客户端输入。
- 不要持久化未经校验的原始 AI 输出。
- AI 接口必须做限流和成本控制。
- 记录 AI 输出校验失败和工具调用失败日志。

## 开发规范

- 优先进行小范围、聚焦的修改。
- 不要重构与当前任务无关的代码。
- 不要在没有明确理由的情况下引入新的大型依赖。
- 在 API、AI 和数据库边界处保持明确的 TypeScript 类型。
- 修改数据结构时，同步更新相关测试、Schema 或校验逻辑。

## 前端UI样式规则

- 优先使用响应式布局，确保内容不会被剪裁，遮盖

## debug & 样式校验
- 需要使用内部预览检验页面时，不要轻易执行 `npm run dev`。
- 在执行 `npm run dev` 之前，必须先检查 `3000`、`3001`、`3002` 端口是否已有可用预览服务。
- 如果任一端口已有服务可访问，直接复用该服务进行页面校验，禁止再次执行 `npm run dev`。
- 只有在 `3000`、`3001`、`3002` 都不可访问，且页面校验确实需要本地开发服务时，才允许执行 `npm run dev`。
- 执行 `npm run dev` 前，必须先说明原因和将使用的端口。
- 不要为每次页面校验重复启动新的 dev server；同一轮任务中应复用已有 dev server。
- 默认不用内置预览检查每个小改动。只有在我明确要求使用预览时，才使用内部预览，并默认切换到 PC 视口，推荐尺寸为 `1440x900`。