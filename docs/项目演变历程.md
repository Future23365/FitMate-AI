# 项目演变历程

## 2026-05-30 Routine 目标时长校验

为解决“用户要 40 分钟训练，但聊天 routine 卡片按实际动作只显示约 25 分钟”的问题，routine 生成链路新增目标时长不足校验。服务端现在会把明显短于目标时长的草稿标记为可恢复失败，并要求自动修复通过增加主训练容量补足，而不是只相信 LLM 写入的 `estimatedSessionMinutes`。

## 2026-05-30 训练草稿校验失败恢复

为解决聊天推送训练计划时 `session_too_long` 等可调整校验失败直接终止的问题，训练草稿生成链路增加了一次服务端自动修复。修复结果仍重新通过 Schema、候选动作和训练规则校验；如果修复失败，前端展示可继续对话的引导和建议回复，不展示未通过校验的训练卡片。

## 2026-05-30 聊天卡片事实源

为解决用户后续引用“这个”“上一个”“刚才那套”时缺少结构化事实源的问题，聊天推送的动作推荐、routine 和 plan 卡片新增 `ConversationArtifact` 持久化层。模型上下文读取 recent artifact summaries，而 `conversationSummary` 继续只保存自然语言摘要；保存 routine 或导入 plan 后会把 artifact 关联到对应来源实体。

## 2026-05-30 聊天引用解析

为解决用户用“这个”“刚才那套”“之前那套练胸的”引用历史训练内容时模型容易猜错对象的问题，聊天链路新增 `ReferenceResolver`。引用解析在服务端基于 recent artifact summaries 和 `ArtifactIndex` 候选集合完成；歧义和未找到时直接返回澄清，不再从 `conversationSummary` 伪造历史卡片。

## 2026-05-30 训练内容局部 Patch

为解决用户只想替换或降低某个动作难度时系统容易重生成整份训练的问题，聊天链路新增 `WorkoutPatch` 和 PatchEngine。ReferenceResolver 已定位 artifact 后，服务端只对被点名动作应用 `artifact_only` 局部修改，替代动作必须来自动作候选集合；成功结果创建新的 artifact revision，saved routine、future schedule 和 completed history 默认 blocked。

## 2026-05-30 AI 编排基础 Trace

为解决 Artifact、ReferenceResolver 和 Patch 接入后错误来源难以复盘的问题，AI trace 从模型调用日志扩展为基础 `AiRunTrace`。现在 `/api/chat` 会记录用户、会话、模型、prompt/tool 版本、recent artifact 摘要和最终决策，并按阶段展示引用解析、受控工具调用、Patch、校验、持久化和响应写入结果；敏感字段和长 payload 会在 trace 层脱敏或截断。

## 2026-05-30 动作元数据分池检索

为解决动作相关性召回无法稳定区分热身、主训练、拉伸和替代关系的问题，动作库新增阶段、角色、运动模式、难度、禁忌和替代关系元数据。候选服务会输出 warmup、training、stretch、regression、progression 和 substitution 分池；routine、plan 和 Patch 保存前再次校验动作必须来自本次候选集合，并且允许进入对应 section。

## 2026-05-30 长期计划领域引擎

为解决长期计划由 LLM 自由铺满日历导致周频率、恢复间隔和递进不可控的问题，聊天链路新增 `PlanStrategy + DomainPlanEngine`。当用户引用已有 routine / plan 并要求“三周都练这个”或“改成一周四练但别太累”时，服务端读取受控 artifact payload，按策略展开 schedule preview，并在返回前校验训练日数量、时长、动作来源和连续负荷。

## 2026-05-30 用户反馈结构化记忆

为解决用户反馈只停留在 `conversationSummary` 中、后续推荐和计划无法稳定复用的问题，系统新增 `UserMemory` 与 `UserExerciseFeedback`。聊天反馈会按长期偏好、动作难度、临时上下文和健康/不适信号分层写入；候选检索、Patch、Validator 和 DomainPlanEngine 读取同一份记忆状态，训练完成率和主观疲劳也会作为后续递进输入。

## 2026-05-30 动作推荐服务端去重

为解决“换一批”仍可能回填当前卡片动作、排除原因不可解释的问题，动作候选服务新增服务端排除集合与 `RecommendationTrace`。当前卡片、`dislike`、健康风险、器械和 section 约束作为硬限制，近期曝光、未来过度使用和 `too_hard` 作为可放宽软约束；推荐 API、聊天动作检索和 Patch 替代候选复用同一套 trace 输出。

## 2026-05-30 RAG 混合检索

为解决“拜拜肉”“核心不稳”“圆肩”“之前那套练胸的”等模糊表达召回不足的问题，RAG 从 prompt 辅助升级为结构化硬过滤约束下的混合检索。Artifact 和 Exercise 维护安全的 `embeddingText` 与本地 embedding；ReferenceResolver 和动作候选服务先执行权限、状态、器械、section、风险等硬过滤，再做全文、向量和业务 rerank，并把 query、过滤、召回、排序与最终候选写入 AiRunTrace。

## 2026-05-30 写操作 Policy 与确认门

为解决 AI 对训练草稿、已保存 routine、未来 schedule 和长期记忆缺少统一写入边界的问题，服务端新增 `PolicyEngine` 与 `ConfirmationGate`。未保存聊天草稿的单动作修改仍可直接创建 artifact revision；覆盖 saved routine、批量 future schedule、改变频率/日历/强度、长期健康限制写入等高影响操作会先返回确认问题和 diff 摘要，确认 token 绑定 userId、目标、scope、operation 和过期时间，写入前必须重新校验。

## 2026-05-30 Trace 调试页补全

为解决 `change-010-ai-trace-eval` 第一版只完成基础展示、仍需要在流程步骤和内容之间来回跳转的问题，`/dev/ai-traces` 改成阶段即内容的纵向调试流。请求概览和阶段模块默认收起，便于先找可疑模块；展开阶段后直接展示事件、字段解释、prompt、候选池、计划草稿摘要和 Raw JSON；阶段内部失败事件和草稿生成事件默认展开，便于排查计划生成、校验和持久化问题。

## 2026-05-30 RAG 模型 Payload 瘦身

为解决 RAG 接入后短对话也可能因为候选动作上下文过宽而消耗过多 token 的问题，动作推荐和训练草稿模型输入改为精简候选摘要。图片、英文名、长推荐原因和展示字段保留在服务端完整动作对象中，不再进入模型 prompt；训练草稿单类候选上限从 40 收敛到 16，降低重复候选池带来的上下文膨胀。

## 2026-05-30 动作推荐追问门控

为解决意图解析已经给出建议追问、服务端却仍触发动作推荐的问题，聊天动作推荐门控新增“追问优先于推送”的边界。`exercise_recommendation` 在 `canTriggerAction = false` 且存在 `suggestedReplies` 时不再兜底触发内部动作，而是保留建议回复；目标明确且无需追问的纯动作推荐仍可直接推送。

## 2026-05-31 AI Token 预算编排

为解决多阶段 AI 编排后 token 成本来源不透明、候选动作 payload 偏宽、summary 更新默认每轮触发的问题，服务端新增 `token-budget` 决策层。聊天、动作推荐和训练计划生成现在会记录本轮 AI 阶段执行/跳过状态、prompt modules、模型可见上下文和候选裁剪摘要；动作候选传给模型前收敛为白名单字段，确认、取消、换一批等无新长期事实的短操作可以跳过 summary LLM 更新。

## 2026-05-31 手动 LLM 黑盒流程测试

为解决手动 LLM 测试只验证内部 prompt 分支、不能代表首页聊天真实体验的问题，`npm run test:llm` 改为首页聊天 3 轮黑盒流程验收。新执行器复用服务端聊天编排和 NDJSON stream，只断言 assistant 用户可见文本、内部字段不泄漏以及 `exercise_recommendation`、`workout_routine`、`workout_plan` 卡片类型是否按预期出现；报告改写到 `docs/manual-llm-blackbox-flow-latest-report.md`。

## 2026-05-31 聊天黑盒流程回归修复

为解决最新黑盒报告中“今天想练胸”不推推荐、“改成45分钟”丢失最近 routine、“我有哑铃”误触发训练和“6 天训练计划”被追问阻断的问题，聊天链路新增服务端意图归一化。模型仍只看 `conversationSummary + latestUserMessage`，但服务端会对纯目标推荐、孤立条件事实、短指令调整和长期计划短语做确定性收敛，并让无显式引用标记的当前训练调整跳过 artifact 语义检索歧义分支。

## 2026-05-31 聊天黑盒流程二次回归修复

为解决黑盒报告中剩余的推荐细化误升级、空泛每周计划过早推送、器械事实不继承问题，聊天归一化层进一步拆分“最近推荐上下文”“durable condition facts”和“可触发长期计划”三类边界。现在“推荐几个不用器械的”会保持动作推荐，“给我一个每周训练计划”会先追问核心条件，“我有哑铃”能被下一轮“今天练胸30分钟”继承为 routine 器械条件。

## 2026-05-31 旧 artifact 引用恢复

为解决聊天自动保存创建新 artifact revision 后，后续长期计划生成仍携带旧 `artifactId` 导致 `getArtifactPayload` 受控读取失败的问题，artifact service 新增 active revision 解析读取入口。长期计划 DomainPlanEngine 分支现在会把当前用户同一 lineage 的旧 id 恢复到 active payload，再生成计划；无法恢复时仍保持可恢复失败，不从 `conversationSummary` 重建训练内容。

## 2026-05-31 序号动作讲解引用修复

为解决黑盒 `F15` 中“第一个动作怎么做”被误判成新动作推荐的问题，聊天链路新增序号动作讲解边界。服务端会把“第 N 个动作”解析到最近 active artifact，读取完整 payload 中展示顺序对应的 `exerciseId`，再从动作库读取详情生成确定性讲解；LLM 不获得任意数据库查询能力，也不会再因此推送新的训练卡片。

## 2026-05-31 移除医疗健康风险决策边界

为避免系统基于疼痛、伤病、不适或动作风险标签替用户决定能不能训练，健康/医疗信号不再参与动作候选排除、训练计划生成、Patch 替换拒绝或长期记忆确认。相关字段和历史枚举保留为兼容数据，但服务端只继续执行动作库、候选集合、结构化校验、权限隔离等非医疗边界；自然语言回复仍不得提供医疗诊断或治疗承诺。

## 2026-05-31 统一聊天意图决策流

为解决聊天回复、内部动作、前端 trigger 和训练计划接口各自解释意图的问题，聊天主链路新增 `ResolvedChatIntent` 执行契约。`/api/chat` 现在先完成 resolved intent、门控、必要 repair、artifact 生成和校验，再生成最终回复；前端只展示服务端返回的 artifact 或失败恢复，不再从正文 trigger 或 `assistant_action` 二次调用生成接口。长期计划 `PlanStrategy` 同步记录字段来源和默认假设，防止默认 21 天等兜底值覆盖用户明确周期。

## 2026-05-31 聊天黑盒运行时错误修复

为解决黑盒报告中纯动作推荐在 stream 前崩溃、笼统每周计划只补频率时过早推送 plan、报告轮次缺失的问题，聊天动作上下文开始保留完整 `Exercise` 作为服务端事实源，模型可见候选仍保持裁剪字段；长期计划门控区分已有 plan 调整和 cadence-only 补齐；手动 LLM runner 会把失败后的同流程剩余轮次记录为 skipped，便于人工判断真实失败数量。

## 2026-05-31 建议回复与执行意图解耦

为解决非执行场景中 `workoutIntent:null` 导致整包意图校验失败、合法 `suggestedReplies` 被兜底清空的问题，聊天意图解析拆分为交互层恢复和执行层强校验。服务端现在会保留合法结构化按钮，并在执行型请求缺失合法 `workoutIntent` 时阻止内部动作；trace 同步记录归一化、部分恢复、最终可见建议回复和被清空原因。

## 2026-05-31 只读 LLM 工具调用

聊天模型过去只能依赖 `conversationSummary`、recent artifact 摘要和固定服务端分支回答复杂追问，无法按需补查真实 artifact 或动作库。现在新增了受控只读工具层和 `runReadonlyToolLoop`：LLM 只能通过 JSON decision 选择 `searchArtifacts`、`getArtifactPayload`、`getExerciseById`、`searchExercises` 四个读工具，服务端负责白名单、Schema、权限、预算、摘要和 trace。

该能力默认由 `ENABLE_READONLY_LLM_TOOLS` 关闭；开启后也只进入历史 artifact 解释、动作详情补查、推荐理由解释和计划理由解释，不改变生成型训练计划、routine、动作推荐或 Patch 的执行决策。AI Trace 新增 `tool_decision`，用于复盘跳过原因、额外模型调用、工具执行和上下文截断。

## 2026-05-31 长期计划周频上下文修复

为解决黑盒 `F05` 中“每周4练，每次45分钟”后再补“增肌，有健身房器械”仍被降级成动作推荐的问题，会话事实抽取现在把 `每周N练` 和 `每周N次` 统一识别为 `weeklyFrequency`。长期计划补齐门控不再依赖模型在最后一轮稳定返回 plan，只要历史结构化上下文已有周频和时长，目标与器械补齐后就会触发 `workout_plan`。

## 2026-05-31 训练校验确定性边界

为解决动态关节活动被静态 section 元数据误判导致 routine 生成失败的问题，训练草稿校验从“动作阶段语义硬编码”收敛为“确定性事实边界”。动作 ID、候选集合、Schema、必要结构、时长和训练量异常仍会 hard fail；`section_exercise_mismatch` 改为 trace warning，保留 AI section 与本地元数据 section 诊断信息，但不再阻止合理草稿展示或进入恢复失败提示。

## 2026-05-31 统一 AI 建议协议

为解决聊天建议分散在 `suggestedReplies`、`clarificationReplies`、`adjustmentReplies`、artifact 失败恢复和 patch 确认中的问题，聊天主链路新增统一 `assistantSuggestions` 协议。服务端集中完成来源标记、去重、阻断优先级和用户口吻过滤，流事件优先输出 `assistant_suggestions` 并保留旧 `suggested_replies` 兼容；前端点击建议时发送 `message` 字段，避免把展示用 `label` 或助手追问文本当成用户输入。

## 2026-05-31 居家背部 routine 触发修复

为解决黑盒 `F04` 中“今天在家练背30分钟”只回复文字、不推送 `workout_routine` 的问题，聊天意图归一化层新增完整单次训练请求边界。当前消息同时包含训练目标、单次时长和场地或器械条件时，服务端会直接收敛为 `routine` 并清空阻断字段；纯动作推荐和长期计划语义仍走原有分支。

## 2026-05-31 本地匿名鉴权

为解决固定 `local-demo-user` 导致不同浏览器数据混用的问题，系统新增浏览器本地匿名身份。前端在应用挂载前创建或恢复匿名 token，统一请求层自动携带凭证；服务端 Route Handler 先解析请求级 `CurrentUser`，再调用聊天、训练、动作库、AI 生成和开发 trace 服务。后续接入正式注册登录时，业务层仍只依赖已校验的用户上下文。

## 2026-05-31 本地匿名 Cookie 与登录弹窗

为解决本地匿名鉴权整页阻塞和 `localStorage` token 暴露的问题，匿名 token 改为服务端写入 HttpOnly cookie。应用启动不再自动创建匿名用户，统一请求层只在私有接口返回明确未认证语义时触发 auth-required 事件，`LocalAuthProvider` 在当前布局内打开 shadcn/ui Dialog；设置页重置只清除当前浏览器 cookie，不删除服务器上的旧匿名用户数据。

## 2026-05-31 本地用户重置软删除

为解决设置页重置文案和服务端行为不一致、旧 cookie 仍可能恢复旧匿名身份的问题，用户生命周期新增 `User.deletedAt`。本地用户重置现在会二次确认、清除当前浏览器 cookie，并把旧匿名用户标记为软删除；恢复会话和私有 API 当前用户解析统一把软删除用户视为未认证，旧聊天、训练、artifact 和记忆数据保留用于审计、调试和未来迁移。

## 2026-05-31 数据库时间格式标准化

为解决数据库 `TIMESTAMP(3)`、本地时间字符串和无 offset fixture 带来的跨时区歧义，所有具体时间点 `DateTime` 字段统一映射为 PostgreSQL `timestamptz` 语义。新增 forward migration 按 UTC+0 解释旧值并转换列类型，服务端 DTO、AI trace、聊天历史和训练持久化边界统一输出 ISO 8601 UTC `Z` 字符串，测试增加 schema、migration 和 fixture 扫描防线。

## 2026-05-31 训练合理性 Hard Fail 移除

为解决用户明确要求连续多天重复当前 routine 时，服务端因 `day_similarity_high`、`consecutive_load_high` 等合理性判断阻止计划卡片展示的问题，训练草稿校验边界再次收窄。`errors` 只保留动作来源、候选集合、必要结构、payload 计数和用户明确约束；训练量、恢复、重复度、section 语义和未确认历史偏好只进入 `warnings`。时长和周频率校验新增字段来源门控，默认值和 LLM 推断值不再反向造成 hard fail。

## 2026-05-31 详细 LLM 测试套件

为解决首页聊天黑盒测试只有 9 个基础冒烟流程、难以覆盖完整首页能力的问题，测试入口新增显式详细模式。`npm run test` 仍运行普通基准测试，`npm run test:llm` 保留基础 LLM 黑盒流程，`npm run test --detail` 才运行详细 LLM 套件；详细套件首版包含 38 个三轮流程，并输出独立报告，避免真实模型成本和报告覆盖影响日常测试。

## 2026-06-01 详细 LLM 黑盒测试强化

为解决详细套件仍偏轻量、无法证明真实首页请求和 artifact 引用链路的问题，LLM 黑盒 runner 改为通过 `/api/chat` Route Handler、测试匿名 current user 和会话保存 Route Handler 执行。每轮成功后会保存消息、summary、context 和训练卡片 payload，并从 `ConversationArtifact` / `ArtifactIndex` 回读诊断；详细 fixture 扩展到 53 个三轮流程，报告开始区分卡片断言、语义断言、失败等级、preflight、token 校准和 artifact payload 读取状态。

## 2026-06-01 12:10:02 CST 聊天历史服务端 Hydration

为解决真实首页请求只带最新消息时服务端 action gate 丢失结构化历史事实的问题，`/api/chat` 请求准备阶段改为按 `conversationId + current user` 读取已保存会话和 current-session artifact summaries。已保存 `messages`、`conversationContext`、`recommendationIntents` 和 recent artifact summaries 优先进入服务端 `internalConversationContext`，客户端上下文只作为 fallback；trace 同步记录 hydration source、周频、单次时长和 recent artifact kind。确定性动作讲解新增引用诊断流事件，黑盒报告可以区分 reference resolved、payload 可读和真实读取失败。

## 2026-06-01 13:09:23 移除服务端语义意图归一化

为解决“跳绳换成别的”等局部替换被服务端关键词规则改写成整套 routine 重新生成的问题，聊天主链路移除了会覆盖 LLM 高层意图的服务端语义归一化。现在 LLM 负责自然语言语义判断，服务端只保留契约校验、引用解析、权限隔离、候选来源和 patch 范围校验；fallback 只产生安全非执行结果，引用型 action 缺少 `workoutIntent` 时不再被当作生成型字段缺失。

## 2026-06-01 14:20:58 CST 详细 LLM 黑盒套件去重与提速

为解决详细 LLM 黑盒套件 53 个三轮 flow 难以日常排查、且存在严格重复和高重叠用例的问题，测试体系新增 fixture 元数据、重复矩阵、子集筛选、失败报告重跑和 flow 级并发队列。报告现在会记录完整 fixture 数、本次筛选数、筛选条件、未运行原因、并发数和去重检查摘要；token 预估和跳过报告改为基于本次实际运行集合动态计算，不再依赖脚本硬编码规模。

## 2026-06-01 14:47:06 CST 动作替换候选选择链路修复

为解决“把某个动作换成别的”只在正文列候选、下一轮选择候选后又降级为 `answer_only` 的问题，聊天 Patch 链路新增短期 `pendingReplacementSelection`。服务端现在会用 `assistantSuggestions` 输出可点击替换候选，下一轮只在 pending 候选集合内消费用户选择，并通过 `WorkoutPatchEngine` 生成 `replace_exercise` 修订卡片；未执行 Patch 的回复不再承诺训练内容已更新。

## 2026-06-01 17:22:56 CST Agent Loop 阶段 4 运行时

为让 Tool-first Agent 在切换 `/api/chat` 前具备可测试闭环，系统新增独立 `runAgentOrchestrator` 运行时、依赖图、checkpoint/replay fixture 和 `AgentExecutionResult` Response Writer 投影。Agent prompt/token 观测合同同步迁移为 `agent_context_build`、`agent_tool_decision`、`agent_tool_execution`、`agent_final_result`、`agent_response_writer` 等阶段，trace 调试页可以分组展示 Agent context、工具决策、工具结果和最终结果。

## 2026-06-01 17:44:46 CST Tool-first Agent 主链收尾

为结束 intent-first 与 Tool-first 双主链并存，`/api/chat` 生产入口固定进入 `AgentOrchestrator`。旧 `ResolvedChatIntent`、`assistant_action`、ReferenceResolver-first、只读-only tool loop 和 summary-only prompt 只能作为迁移期兼容或测试夹具存在；黑盒 runner 改为从 `AgentExecutionResult`、tool dependency graph、artifact/patch/suggestion 事件和 legacy path skip 判断真实执行闭环。

## 2026-06-01 18:19:35 CST Agent Trace 诊断页重构

为解决 `/dev/ai-traces` 仍按旧 intent-first 顺序排查 Tool-first Agent 的问题，调试页新增 `AgentTraceViewModel`。页面现在默认展示 Agent run 总览、phase flow、tool timeline、resource links 和 diagnostic findings，并在保存全链路 log 时追加 Agent 诊断摘要；legacy trace 仍按旧流程展示，并明确标记“未记录 Agent run”不是业务失败。

## 2026-06-01 18:37:13 CST Agent blocked 与动作发布态修复

为解决发布态动作检索召回为空后又因 `blocked` 结果缺少 `blockReason` 而退化为通用失败文案的问题，动作种子数据统一设为发布态，seed 默认写入 `isPublished: true`。Agent final result prompt 增加 `blocked` 合同示例，解析层只对旧形态 `blocked.replyContext.reply` 做字段迁移，保证候选为空等可解释停止能被 Response Writer 投影为用户可见阻塞原因。

## 2026-06-01 19:13:10 CST 旧意图架构移除

为结束 Tool-first Agent 与旧 intent-first 残留代码并存，生产 `/api/chat` 移除了旧 `ResolvedChatIntent`、旧 action gate、独立只读 tool loop 和旧 `assistant_action` / `intent_resolved` 流事件派生。前端和黑盒 runner 现在只消费 `AgentExecutionResult`、tool dependency graph、artifact / patch / suggestion 事件和 done metadata；trace 记录旧 intent、旧 normalize、summary-only、ReferenceResolver-first、readonly loop 和旧 action event 的缺席证据。

## 2026-06-01 19:50:42 CST Agent 推荐卡片与 Token 预算修复

为解决 Tool-first Agent 推荐动作时只回复文本、不推送卡片、建议按钮消失以及单轮请求 token 飙升的问题，聊天主链新增基于 `searchExercises` tool result 的推荐卡片投影。Response Writer 统一补齐前端 `assistantSuggestions` 协议字段，Agent decision 请求改用瘦身模型输入，并为模型响应 trace 补充 Agent stage metadata，使 token 分账回到 `tool_decision` 阶段。

## 2026-06-01 20:11:09 CST Agent Decision Schema 摘要修复

为解决 token 瘦身后模型看不到数组枚举而反复传错 `allowedSections`、并把澄清工具写成非法 `action` 的问题，Agent decision 的模型可见 schema 摘要保留数组 item 类型和枚举。解析层新增已注册工具名 action 的结构规范化，让 `askClarification` 这类输出回到合法 `call_tool` 合同，同时继续由原工具 Zod schema 执行边界校验。

## 2026-06-01 20:14:16 CST Agent Loop Trace 可读化

为解决 Tool-first Agent trace 仍需要人工从阶段事件和 Raw JSON 拼接因果链的问题，trace 合同补齐 `loopTurnId`、`modelCallId`、`toolCallId`、`toolResultId`、可见/已用 tool result 和关键 resource id。调试页新增 Agent loop timeline，默认展示每轮 LLM 输入、LLM 输出解析、工具执行结果、下一轮输入可见性和最终收口；保存全链路 log 复用同一 view model，用户问答记录继续保持窄格式并由 API 层兜底过滤完整 prompt、tool payload 和卡片数据。

## 2026-06-01 22:25:35 CST Agent 动作 Facet 契约修复

为解决“上肢 + 哑铃 + 30 分钟” routine 请求被 `searchExercises` 误筛成 0 候选的问题，Agent 动作检索工具新增 `bodyRegions` 结构化字段，并把 `targetMuscles` / `equipment` 收紧为动作库真实 facet 边界。服务端只根据结构化身体区域做确定性展开，不读取用户原文重解释语义；当模型仍传入 `upper body` 这类未知 facet 时，工具会基于 diagnostics 做一次恢复查询，避免可修正的候选检索失败直接变成 `blocked`。

## 2026-06-01 22:32:35 CST Agent Routine 与动作推荐卡边界修复

为解决 facet 恢复后 Agent 仍把“安排一套训练”当动作推荐、正文输出自由文本编排但卡片展示动作推荐的问题，Agent 决策提示和 `searchExercises` 工具摘要补充 routine 工具链硬约束：单次训练编排必须走 `candidateUse="routine"` 并继续调用 `generateRoutineDraft`。Response Writer 同步收窄动作推荐卡投影，只允许 `candidateUse="recommendation"` 的 `answered` 结果生成推荐卡，并避免卡片 summary 复制整段聊天正文。

## 2026-06-01 22:39:49 CST Agent Routine Intent 合同兜底

为解决 Agent 已正确调用 `generateRoutineDraft` 后仍因缺少 `experience` 和 `weeklyFrequency` 失败的问题，routine 工具入口新增结构合同默认值：缺少经验水平时按 `beginner` 生成保守训练量，缺少周频时用 `1` 满足复用校验模型字段。Agent final result 解析层同步兼容旧式 `failed.replyContext.reply`，避免真实工具失败被二次覆盖成 `model_output_invalid`。

## 2026-06-01 22:52:59 CST Agent Routine Draft 资源合同修复

为解决 `generateRoutineDraft` 成功后 `validateRoutineDraft` 因模型未回传完整 draft payload 而失败、最终退成纯文本回答的问题，Agent runtime 开始在服务端 tool result 中保存完整 output。后续 routine validation、Policy 和 artifact revision 写入通过 `draftId`、`validationId`、`policyDecisionId` 解析本轮已登记资源，模型只负责引用资源 id，避免复写训练 payload 导致卡片生成链路中断。

## 2026-06-01 23:19:49 CST Agent 黑盒链路收口修复

为解决 Tool-first Domain Agent Orchestrator 主链在最新首页 LLM 黑盒报告中出现的推荐卡、routine 卡和 plan 卡收口失败，服务端补齐了 Agent tool result 到用户可见 artifact 的闭环。推荐卡片可从本轮 recommendation candidate set 兜底投影，首次 routine/plan 可在无 source artifact 时创建新的 `ConversationArtifact`，首次 plan 生成可用候选动作构造 seed routine 后交给 `DomainPlanEngine` 展开；Validator 同时修正恢复日被误判为训练时长不足的问题，黑盒 runner 也不再把已出训练卡的同轮 `answer` 状态当作额外卡片。

## 2026-06-01 23:32:39 CST Agent Routine Draft 误传字段兜底

为修复真实 trace 中 `generateRoutineDraft` 已成功但 `validateRoutineDraft` 因模型误传 partial `draft` 而 schema 失败的问题，validation 工具合同移除了模型可见的 `draft` 输入字段。后续 routine/plan 校验只通过 `draftId` 读取本轮服务端 tool result 中的完整 draft，模型误传 payload 不再能抢先触发 schema 失败，从而保护 `validateRoutineDraft -> evaluatePolicy -> saveConversationArtifactRevision` 的卡片生成闭环。

## 2026-06-01 23:41:41 CST Routine 候选 Query 门控修复

为修复最新 trace 中 `searchExercises(candidateUse="routine")` 因 `query="上肢训练"` 返回 `no_hybrid_match` 的问题，动作搜索将 routine/plan 的结构化候选边界提升为可执行候选事实源。`bodyRegions`、`equipmentRequired`、`allowedSections` 等 hard filters 已经命中候选时，泛化 query 不再作为硬召回条件清空结果；recommendation、answer 和 patch 搜索仍保留原 query 召回语义。

## 2026-06-01 23:49:47 CST Agent Policy Schema 摘要修复

为修复最新 trace 中 `validateRoutineDraft` 成功后连续三次 `evaluatePolicy` schema validation failed 的问题，Agent decision 模型输入瘦身开始支持 `oneOf` / `anyOf` union schema 摘要。`evaluatePolicy` 的 `new_artifact` 分支现在会向模型暴露 `policyTarget="new_artifact"`、必填 `artifactKind` 和必填 `draftId`，同时 prompt 明确首次创建 routine/plan artifact 的 policy 调用形态，避免模型把该工具误判为无输入工具。

## 2026-06-02 00:03:01 CST Agent Save Null Optional 修复

为修复最新 trace 中 `evaluatePolicy` 已成功但 `saveConversationArtifactRevision` 因 `sourceArtifactId: null`、`payload: null`、`patchId: null` 等可选字段 schema validation failed 的问题，保存工具开始把 optional absence 的 `null` 归一为未提供。首次 routine/plan 保存仍通过 `draftId` 从服务端已登记 draft 解析 payload，缺少 draft/payload、缺少 `artifactKind` 或 Policy 不允许写入仍会失败，避免把 LLM 的空值表达误判成业务失败。

## 2026-06-02 00:14:21 CST Agent Premature Final Result 修复

为修复最新 trace 中候选、draft、validation 和 policy 都成功后，模型却在保存前提前返回非法 `final_result.generated` 的问题，Agent runtime 新增保存前提前 final 的可恢复反馈。当前 run 已具备 `draftId + validationId + policyDecisionId` 且尚未有 `revisionId` 时，非法 `generated` 不再直接结束为 `model_output_invalid`，而是写入 `agentDecisionFeedback`，提示下一轮继续调用 `saveConversationArtifactRevision`；只有保存成功拿到真实 `revisionId` 后才能返回 `generated`。

## 2026-06-02 00:25:50 CST Agent Generated Final Result 合同修复

为修复最新 trace 中 `saveConversationArtifactRevision` 已成功保存 artifact、但模型最终 `final_result.generated` 仍只返回 `replyContext` 而缺少 `artifact`、`revisionId` 和 `validationId` 的问题，Agent runtime 新增保存后终止合同补齐。补齐字段只来自本轮已登记保存 tool result，并继续执行 final result 引用校验；prompt 同步加入 `generated` 完整 JSON 示例，避免已保存训练编排在最后一步被误降级为 `model_output_invalid`。

## 2026-06-02 00:44:26 CST Agent 推荐 FinalResult 修复

为修复真实 trace 中 `searchExercises(candidateUse="recommendation")` 已成功返回弹力带臀腿候选、但模型最终 `final_result.answered` 漏掉顶层 `reason` 后整轮退成 `model_output_invalid` 的问题，Agent decision 解析层新增只补非语义诊断字段的容错。动作推荐搜索也将结构化候选边界提升为事实源：`bodyRegions`、`equipment`、`level` 等 hard filters 已命中时，泛化 query 只参与排序，不再清空候选；`searchExercises` 工具定义同步补充真实 facet 摘要，帮助模型优先传数据库可执行条件。

## 2026-06-02 01:01:15 CST Agent 活动状态条

为解决 Tool-first Agent 请求期间用户只能看到“正在思考”、无法感知编排推进的问题，聊天 stream 新增 UI 安全的 `agent_activity` 事件。服务端在上下文准备、需求分析、动作库查询、artifact 读取、训练生成、校验、保存、回复整理和收尾等稳定边界发出粗粒度阶段；前端在当前 AI 回答框顶部展示中文状态条，并用 sequence 防止旧阶段覆盖新阶段。activity 不进入聊天消息、历史保存、summary、conversation context 或 artifact payload，原有“正在思考”回答加载提示继续显示在状态条下方。

## 2026-06-02 01:29:43 CST Agent Routine 候选覆盖修复

为解决真实 trace 中用户指定 8 个弹力带动作但 routine 最终只保留 2 个动作的问题，`generateRoutineDraft` 从“三段各挑一个动作”改为“指定候选全部保留，缺阶段再受控补齐”。主训练 section 现在可以包含多个动作项，补充热身或拉伸动作会进入扩展后的候选边界并继续由 Validator 校验；动作元数据也修正了 `髋伸展 / hip extension` 被误判为拉伸的问题，避免力量动作被错误放入 stretch。

## 2026-06-02 12:28:07 CST Agent JSON Decision 恢复

为解决同一提问有时能出 routine、有时因模型末尾多输出一个 `}` 而失败的问题，Agent JSON 解析层新增非语义对象边界恢复。恢复只处理尾随字符、fenced JSON 和包裹文本中的唯一 JSON object，恢复后仍必须通过 `AgentToolDecision` Schema、registry 和工具输入校验；trace 同步记录 `parseStatus: "recovered"` 与原始严格解析失败，避免把格式波动误判为训练生成失败。

## 2026-06-02 12:40:48 CST Agent Recommendation Artifact 绑定 Routine 修复

为解决用户要求“就显示的 8 个动作生成训练”时 Agent 重新裸搜无关动作的问题，Agent 上下文开始保留 recent artifact 的轻量 `exerciseIds`。`generateRoutineDraft` 新增 `sourceArtifactId` 和 `requiredExerciseIds` 契约，绑定推荐卡片时服务端只校验结构化 artifact 来源和 required 动作覆盖，不用关键词判断用户语义；缺少热身或拉伸时仍由服务端受控补齐，并把补充动作纳入最终候选边界。

## 2026-06-02 13:35:50 CST 旧聊天 AI 接口清理

为结束 `/api/chat` Agent-first 主链和旧聊天 AI 独立接口并存的问题，旧计划草稿 route、旧动作推荐 route、前端旧 route helper、旧 trigger JSON parser、旧 reference resolver service、旧 workout patch chat service 和旧 shared referenceResolution schema 被移除。聊天计划、routine、推荐、Patch 和建议回复现在只能来自 `AgentExecutionResult`、tool result、artifact / patch / suggestion 事件或已存在 Agent result 的确定性操作；新增架构级扫描测试防止旧执行面和旧主规格要求回流。

## 2026-06-02 14:13:19 CST Agent Artifact Revision 与 Tool Loop 稳定化

为解决用户要求“这8个动作做成一套训练”时 recent artifact summary 可见但完整 payload 因旧 revision 变成不可访问的问题，Agent artifact 读取和 artifact-bound routine 生成改用同 lineage active revision 恢复。`requiredExerciseIds` 仍必须来自恢复后的推荐 payload；runtime 同时新增重复不可重试工具失败熔断和模型可见失败摘要压缩，避免同一个 `getArtifactPayload` 失败在 tool loop 中反复消耗 token。Trace 现在记录 requested/active artifact id 和 duplicate failure 证据。

## 2026-06-02 17:43:46 CST Tool-first 工具能力合同收紧

为解决执行型 `searchExercises` 只能“搜到候选”但不能证明“满足无器械、阶段覆盖、候选来源”等硬合同的问题，Agent 工具体系新增统一 `AgentToolCapabilityContract` 和 ToolResult fulfillment。`searchExercises` 现在必须用结构化 `operation/filters/resultRequirements` 构建候选集合，并输出 `candidateSetEvidence`；routine、plan、patch、validator、policy 和保存链路只能消费当前 run 中 `satisfied=true` 的上游资源。引用解析和记忆查询也拆成 `resolveArtifactReference` 与 `queryUserMemory`，避免候选搜索或 snapshot 被误当作精确事实源。

## 2026-06-02 18:16:05 CST Agent 合同修复循环

为避免保存前 final、未登记 `revisionId`、工具 schema 失败和重复工具失败继续散落成局部补丁，Agent runtime 新增统一 `AgentDecisionFeedback` 修复协议。可恢复合同失败会以模型可见摘要进入下一轮，并受 repair turn、同类 feedback 和 step 预算约束；失败 tool result 与 `satisfied=false` 只作为错误事实登记，不再产生可消费业务资源。最终 `generated`、`patched` 和 `completed_operation` 只从当前 run 已登记写工具结果或可验证投影收口，trace 与黑盒报告同步记录 recovered、fused、unrecoverable 和 projected 证据。

## 2026-06-02 18:29:27 CST Agent Tool Schema 嵌套摘要修复

为解决复杂 Agent Tools 在模型侧只暴露顶层字段名、导致 LLM 把对象合同误写成数组或错误嵌套的问题，Agent decision 模型输入的 schema 摘要改为受控递归结构。工具摘要现在能表达 object、array、record / `additionalProperties` 和 union 分支的关键子结构，例如 `searchExercises.resultRequirements.sectionCoverage.<section>.min`、`softPreferences` 的合法子字段、`generateRoutineDraft.intent`、`generatePlanDraft.strategy` 和 `evaluatePolicy` 分支必填字段；真实工具 Zod Schema 和执行边界保持不变。

## 2026-06-02 20:13:13 CST Agent Runtime 资源合同稳定化

为解决 partial candidate、failed/feedback 诊断、澄清工具和执行型 routine 链路在 final result 引用上边界不清的问题，Agent runtime 新增 `consumable`、`diagnostic`、`partial`、`feedback` 资源角色。写入类终止结果只能消费当前 run 的成功 producer；诊断结果只能用于 `answered`、`blocked`、`failed`、`needs_clarification` 的说明证据。`askClarification` 成功后现在稳定投影为 `needs_clarification`，`searchExercises` 未满足 section 覆盖时返回 partial 诊断并提供恢复建议，routine warmup / stretch 可用数据库无器械受控补充动作补齐，trace 和黑盒报告同步展示资源角色与防逃逸证据。

## 2026-06-02 20:17:05 CST 本地动作图片展示 URL

为解决动作图片已下载到 `exercises_picture/` 但前端仍默认展示 GitHub raw URL 的问题，服务端新增动作图片配置、manifest resolver 和受控图片读取 Route。动作库、推荐卡和已保存 routine / schedule 读取链路现在统一派生 `/api/exercise-images/...` 或配置后的外部资源 URL，数据库中的原始图片来源字段保持不变，后续迁移 CDN 不需要重写事实数据。

## 2026-06-02 20:31:54 CST Agent Routine 分段器械边界修复

为解决用户已确认“热身/拉伸无器械、主训练用哑铃”后仍生成失败的问题，routine draft builder 开始消费 `candidateSetEvidence.controlledSupplementalCandidates` 的 section 证据。受控补充为 warmup 的动态拉伸动作不再被通用元数据重新挪到 stretch；缺失 warmup / stretch 时默认优先无器械或自重补齐，training 仍保留主训练器械优先级。这样把旧规格中的“普通器械只默认约束主训练”落实为服务端可测逻辑，而不是只依赖 prompt。

## 2026-06-02 21:27:31 CST 聊天 Artifact 查看与建议边界收紧

为解决短回复进入执行失败、查看刚才训练不重推卡片、未开放保存建议暴露、同气泡 artifact 重复 active 以及 raw text goal 摘要污染的问题，Agent 聊天链路把修复点收敛到结构化合同。Response Writer 现在按 `targetOperation` 和产品能力过滤建议，不按文案关键词判断；`answered + getArtifactPayload` 可把 routine / plan payload 只读重投影成当前气泡卡片；写工具使用 runtime context 注入的 `responseMessageId` 绑定 artifact；artifact service 对同 message、稳定 payload 和 recent active index 做去重；上下文摘要不再从 raw user message 提取高层 goal。

## 2026-06-02 21:29:11 CST 前端首包与动作资源性能优化

为解决首页聊天、动作编排、动作图片和全局 layout 的首屏成本问题，前端新增 `.next` manifest 性能统计流程，并把首页 Markdown、artifact cards、动作详情 Sheet 和主应用 shell 副作用拆到真实需要的边界。动作列表接口改为 `ExerciseListItem` 摘要响应，完整详情按 id 读取并缓存；本地动作图片改走 Next image optimizer。生产统计中首页 gzip 从 179.9 KB 降至 59.1 KB。

## 2026-06-02 22:19:41 CST Agent 默认无器械候选合同

为落实“未指定器械默认无器械”的产品规则，Agent 执行型 `searchExercises` 在缺少正向可用器械、居家条件或已确认器械事实时，会结构化补入 `homeRequirements=["no_equipment"]`。routine 和 plan 生成工具会在候选 evidence 证明无器械时同步把 intent / strategy 标记为自重默认；通用动作搜索服务保持不变，避免动作库页面、composer 和普通 `/api/exercises` 查询被 Agent 默认污染。

## 2026-06-02 22:24:06 CST 动作库服务端分页查询

为解决动作库列表和动作编排页右侧动作库仍在服务端全量读取 `Exercise[]` 后再分页的问题，`/api/exercises` 的列表查询下推到 repository 的数据库分页路径。列表响应只读取 `ExerciseListItem` 摘要字段，facets 改用数据库聚合，详情接口和 AI `searchExercises()` 的完整动作事实读取保持独立，避免普通筛选请求占用完整动作库内存。

## 2026-06-03 10:55:56 CST 旧 Agent Core 运行时删除

为结束旧聊天 AI/Agent 运行时继续误导后续实现的问题，本次删除旧 Agent core、tool registry、Prompt、模型调用、Response Writer、activity 事件、手动 LLM runner 和旧核心链路测试。`/api/chat` 保留鉴权、请求校验和历史 hydration，但返回明确的 `chat_ai_disabled` 禁用响应；聊天页面、历史会话、历史 trace 查看器和非 AI 公共领域服务继续保留，后续 AI 聊天能力需通过新规格重新设计。

## 2026-06-03 11:44:58 CST 聊天旧 Stream 合同收尾

为避免旧 Agent Core 删除后前端仍像在消费 NDJSON Agent stream，本次将首页聊天请求从 `requestChatStream()` 收敛为普通 JSON 禁用响应请求，删除 `ChatStreamEvent`、artifact / patch / reference diagnostic 旧事件类型和 `response.body.getReader()` 解析分支。聊天页面继续保留历史会话、已保存卡片和本地状态，但不再保留旧 stream parser 或旧 Agent 事件消费合同；后续重新接入 AI 时需要重新定义前端事件协议。

## 2026-06-03 12:51:48 CST Agent Tool 合同内核 M0 闭环

为在旧 Agent runtime 删除后重新建立干净的通用内核，本次新增独立 `lib/server/agent-core/**`，打通 `ToolRegistry`、manifest、`PlannerPort`、`ReplayPlanner`、Action Validator、Executor、Observation、Runtime loop 和默认 Response Renderer。M0 只注册只读 fixture tool 做合同测试，不接入 production `/api/chat`、真实 LLM adapter、真实业务 tool、数据库或前端 UI；ResourceStore、Policy Guard、confirmation、Trace/Replay 和生产聊天接入继续留给后续阶段。

## 2026-06-03 13:43:49 CST Agent Tool 安全资源闭环 M1

为让新 `agent-core` 具备接入真实业务 tool 前的安全底座，本次在 M0 只读内核上补齐 `ResourceStore`、Resource Contract、`Policy Guard` 和 confirmation pending action。资源只能在当前 run 内登记和消费，diagnostic 只能作为失败或澄清证据，write/high-risk tool 会先返回服务端生成的 confirmation request，resume 时只执行服务端保存的 pending `tool_call`。M1 仍不接入 production `/api/chat`、真实 LLM、真实业务 tool 或数据库持久化，只通过 fixture 和架构扫描验证通用闭环。

## 2026-06-03 13:59:04 CST Agent Tool M1 安全闭环偏差修复

为修正 M1 实现与架构文档之间的细节偏差，本次将 confirmation pending action 的 `consumed` 标记推迟到 tool handler 成功执行之后；`ResourceStore` 开始生成缺省 resource id 并拒绝重复登记，避免 Planner 或 handler 间接控制资源覆盖；`Policy Guard` 的 dynamic confirmation 改为可注入结构化 evaluator，默认保守确认且不读取自然语言关键词。这样 M1 的资源、确认和动态策略边界更贴近架构文档要求。

## 2026-06-03 15:38:04 CST Agent Tool 上线硬化 M2

为让 M1 安全闭环具备接真实模型前的上线硬化能力，本次新增 `ModelAdapter`、`LlmPlanner` 和 DeepSeek-only adapter，并在通用 runtime 中补齐 manifestHash、registry snapshot、tool manifest linter、统一 redaction、observation 压缩、trace audit、预算和 `idempotencyKey`。M2 仍不接入 production `/api/chat`、真实动作库、训练生成、保存、用户记忆或数据库业务 tool；DeepSeek 协议和环境变量只存在于 `agent-planners`，`agent-core` 继续只依赖 `PlannerPort`。

## 2026-06-03 15:57:06 CST Agent Tool Diagnostic Grounding 补漏

为补齐 M2 diagnostic grounding 的边界缝隙，本次让 `final_answer.usedToolResultIds` 不再只校验 id 是否存在，而是必须引用执行成功且 `fulfillment.satisfied=true` 的 tool result。failed 或 unsatisfied 的诊断结果仍可用于 `ask_user` 解释阻断或追问，但不能支撑 completed final answer。这样 diagnostic resource 和 diagnostic tool result 两条引用路径都被统一纳入成功 grounding 合同。

## 2026-06-03 16:19:54 CST Agent Tool 变更治理流程固化

为避免后续新增业务 tool 或修复 Agent tool bug 时重新把业务分支写回 core，本次新增项目级 `agent-tool-change-governance` Skill，并在 Agent Tool Orchestrator 文档中固化 OpenSpec 前置分类、允许/禁止模块和验证 checklist。测试侧扩展架构扫描、contract helper 和 governance regression，自动检查 `agent-core` 业务污染、`/api/chat` 关键词分流、write/high-risk confirmation、diagnostic grounding 以及完整 tool output 投影泄漏。

## 2026-06-03 16:41:50 CST Agent 文本聊天生产接入

为让首页聊天重新拥有真实 AI 文本闭环，本次将 `/api/chat` 从 `chat_ai_disabled` 禁用响应切换到新 `agent-core`。生产路径使用 `PreparedChatRequest -> AgentRunInput -> 空 ToolRegistry -> LlmPlanner + DeepSeekModelAdapter -> runAgentRuntime -> 默认 Response Renderer -> NDJSON`，前端恢复通用文本流消费，只处理回答文本、澄清建议、错误和完成事件。当前仍不接入动作检索、训练生成、保存 artifact、用户记忆或任何业务 tool，空 registry 下的 `tool_call` 只能按结构化错误收口。

## 2026-06-03 17:14:00 CST Agent 文本聊天 Unsupported 错误投影

为避免空 `ToolRegistry` 下模型返回 `tool_call` 后把 runtime 内部英文错误展示给用户，本次把 production text chat 的错误投影拆成用户可见文案和内部诊断两层。unknown tool、unsupported capability、tool call 不可执行和 repair limit 等能力缺口会基于 registry、runtime error code、trace event 等确定性事实投影为普通中文助手回复和建议；通用 Response Renderer 与前端 chat client 同步改为只展示安全中文兜底。当前仍不接入真实业务 tool，也不通过用户文本关键词判断能力意图。

## 2026-06-03 17:26:56 CST Agent 文本聊天 Trace 接入

为解决新 `agent-core` 文本聊天请求无法出现在 `/dev/ai-traces` 的问题，本次在 `/api/chat` 薄接入层补齐开发态 `AiTrace` 生产。trace 记录 `AgentRunInput`、空 `ToolRegistry`、runtime `traceEvents`、terminal status / error code 和真实 NDJSON 响应摘要；配置错误、runtime 合同失败和成功路径都会留下当前用户可见的脱敏 trace。`agent-core` 主循环、PlannerPort、Executor、Policy Guard、Resource Contract Validator 和默认 Response Renderer 不感知 dev trace store，也不恢复旧 Agent 事件或业务 tool。

## 2026-06-03 17:37:00 CST Agent LLM Prompt 配置抽离

为避免默认 system prompt 继续硬编码在 DeepSeek 请求构造中，本次新增 `lib/server/agent-planners/prompts/agent-llm-prompt-config.ts`，集中声明 `AgentLlmPromptConfig`、默认 prompt、`promptVersion` 和 builder。`DeepSeekModelAdapter` 改为从配置读取 system message，并支持测试注入；`agent-core`、`PlannerPort`、runtime、空 `ToolRegistry` 和 `/api/chat` 外部事件合同保持不变。文档同步标注旧 `lib/server/ai/prompt-config.ts` 不再是当前生产文本聊天 prompt 入口，后续业务能力说明应来自 tool manifest、resource contract、projection 或 observation。

## 2026-06-03 18:12:36 CST Agent LLM Trace Debugger 观测增强

为解决 `/dev/ai-traces` 只能看到 runtime 失败、看不到 LLM 请求响应和真实 token usage 的问题，本次在 Planner / ModelAdapter 边界新增安全诊断。`DeepSeekModelAdapter` 生成脱敏 request / response envelope，`LlmPlanner` 记录 planner call index 与 runtime step，`/api/chat` 将其投影为 `model_request` / `model_response` trace steps。trace viewer 改为按架构模块展示，并在保存全链路 log 中保留 `moduleGroups`、`plannerModelCalls`、`tokenUsageSummary`、runtime events 和 Raw trace；`PlannerPort`、runtime 主循环和用户可见 NDJSON 合同保持不变。

## 2026-06-03 18:26:07 CST Agent 文本聊天基础问答 Prompt 修复

为修复“你能干什么”这类基础问答在空 `ToolRegistry` 阶段被模型非法 action 打成 `repair_limit_exceeded` 的问题，本次将默认 Agent LLM prompt 升级为中文 `AgentAction` 合同。新 prompt 明确空 `tools` 时禁止 `tool_call`，普通问答、能力说明和训练原则解释走 `final_answer`，缺必要信息才走 `ask_user`；模型回答能力边界时不能承诺执行未注册工具。production text chat 的 unsupported fallback 也收窄为不可执行工具调用的安全错误边界，不再用固定业务文案替代模型基础回答。

## 2026-06-03 18:52:05 CST Agent Prompt 合同治理入口

为避免后续 prompt / model input 改动只停留在源文件文案层，本次新增项目级 `agent-prompt-contract-governance` Skill。该入口要求先确认模型实际可见输入，再检查 `AgentAction`、tool loop、resource、policy、grounding、repair 和业务 tool 可见说明是否完整；它与 `agent-tool-change-governance` 分工为“先定 tool/core/production 范围，再审 prompt/model input 合同”。本次只新增 Skill 和协作文档，不修改真实 prompt runtime 或生产聊天链路。

## 2026-06-03 19:00:29 CST Agent 健身业务边界 Prompt 完善

为补齐生产文本聊天 system prompt 中缺失的项目级业务方向，本次将默认 Agent LLM prompt 从纯 `AgentAction` 合同扩展为包含高层产品边界：助手是 AI 健身助手，围绕动作推荐、训练限制整理和训练计划编排提供帮助；同时明确不提供医疗诊断、治疗建议、伤病判断或康复处方。实现只修改模型可见 prompt 和测试，`/api/chat` runtime、空 `ToolRegistry`、业务 tool 注册和服务端语义边界保持不变。

## 2026-06-03 21:14:21 CST searchExerciseResources 身体区域合同修复

为修复“我想练腿”被 `searchExerciseResources(muscle="腿部")` 查成 0 结果后回答“没有腿部动作”的问题，本次为只读动作查询 tool 增加 `bodyRegions` 结构化区域字段，并将 `lower_body` 确定性展开到动作库真实肌群 facet。`muscle` 继续只表示真实肌群值；服务端不读取用户原文做关键词或同义词改写。空命中结果从无条件 `satisfied=true` 改为 `satisfied=false`，避免 0 结果继续支撑成功动作推荐。

## 2026-06-03 21:33:04 CST Agent Tool 执行 Trace 统一记录

为补齐业务 tool 接入后的调试证据，本次在 `agent-core` runtime 增加通用 `tool_execution` trace event。所有通过 Runtime / Executor 执行的 tool 都会自动记录实际 input 摘要、toolCallId、toolResultId、状态、失败 code、projection / fulfillment 摘要、resource 引用和耗时；`/dev/ai-traces` 将它展示为同一 loop 内的 `Tool 执行` step。这样新增业务 tool 不需要各自手写日志点，同时避免完整 handler output 默认进入 trace。

## 2026-06-03 22:00:29 CST searchExerciseResources 精确 Facet 合同修复

为修复 `searchExerciseResources` 模型可见 example 使用不存在的 `homeRequirement="home_friendly"`，导致“无器械居家训练”查询被精确筛成 0 的问题，本次只修 tool 合同和 examples。`homeRequirement`、`equipment`、`level` 现在向模型暴露当前真实 facet 摘要，示例改用 `none` / `无器械`、`body only` 等可执行值；服务端不新增旧值 alias、同义词表或基于用户原文的语义改写。

## 2026-06-03 22:38:42 CST Agent 动作刷新事实桥落地

为支持“再推荐一批 / 换一批”这类跨轮动作刷新，本次将 production `/api/chat` 只读 tool 预算从单次放宽到可完成 `read/import -> query -> final_answer` 的多步链路，并新增 `ConversationBusinessFact` 作为跨 run 业务事实源。上一轮动作事实只从 Response Renderer 的用户投影保存，下一轮只恢复轻量摘要，完整事实必须通过 `readRecentExerciseRecommendationFact` 重新引入当前 run；`searchExerciseResources` 新增 `excludeExerciseIds` 并在数据库层排除已展示动作，避免服务端从自然语言正文反推动作列表或使用关键词分流。

## 2026-06-03 23:00:12 CST Agent 动作刷新 factRef 合同修复

为修复“换一批”在没有 recent fact 时照抄 `cbf_previous_response` 占位引用导致 `handler_error -> duplicate_tool_failure` 的问题，本次收紧 `readRecentExerciseRecommendationFact` 的模型可见合同和 example，要求 factRef / messageId 只能来自当前 run metadata 中真实存在的 `recentExerciseRecommendationFacts`。同时将 fact store 读取异常归一为结构化 failed tool output，不改 `/api/chat` 语义分流，仍由模型决定解释、澄清或继续使用其他可见工具。

## 2026-06-04 00:52:03 CST Agent 模型可见描述中文化

为统一 Planner 实际可见输入，本次将模型可见描述性 prompt 默认中文固化到 `AGENTS.md`、Agent prompt 合同治理 Skill、Agent tool 变更治理 Skill 和 Agent Tool Orchestrator 文档。生产 tool 与 fixture tool 的 `description`、`whenToUse`、`whenNotToUse`、schema description 和 examples description 改为中文说明，同时保留 `toolName`、字段名、enum、action type 和 resource type 等英文执行标识；manifest hardening 增加语言回归，防止英文业务说明重新进入模型可见合同。

## 2026-06-04 01:21:16 CST Agent 动作刷新重复成功收口

为修复“换一个”场景中 `readRecentExerciseRecommendationFact` 成功读取真实 recent fact 后重复同参调用，最终因重复登记 resource 触发 `invalid_action` hard failure 的问题，本次收紧 read/import 与动作搜索 tool 的模型可见合同，并在 agent-core 通用重复调用窄口新增 `duplicate_tool_success` feedback。成功且满足的相同 tool/input 结果会被复用为 Planner 可见反馈，不再执行 handler 或重新登记 resource；changed input 和 `satisfied=false` 结果仍走原有执行、失败反馈或澄清边界。

## 2026-06-04 01:43:47 CST searchExerciseResources 零结果事实 Grounding 修复

为修复“有没有铅球动作”查询 0 条后被 `terminal_reference_invalid` 打成 `repair_limit_exceeded` 的问题，本次将 `searchExerciseResources` 的 0 条合法查询结果重新定义为已完成事实查询，而不是 unsatisfied 诊断结果。`totalMatches = 0` 现在可以支撑“没有找到”的普通 `final_answer`；但该 tool 仍不产出 routine、plan、训练卡片或候选消费资源，真正需要动作候选的下游流程必须自行校验候选数量和适用性。服务端不新增“有没有/推荐”关键词分流，Action Validator 的 failed / unsatisfied grounding 安全边界保持不变。

## 2026-06-04 01:58:25 CST AiTrace 双文件导出

为降低排查 log 时的默认 token 成本，本次将 `/dev/ai-traces` 的「保存全链路log」从单个大文件改为双文件覆盖导出：`codex_logs/ai_trace_log.js` 保存轻量结构化报告和 `contentRef`，`codex_logs/ai_trace_texts.jsonl` 保存脱敏长文本映射。默认先读报告定位 loop、step、tool、error code、token usage 和关键 id；需要 prompt、model input、observation 或 raw response 细节时，再按 `contentRef` 查长文本文件。该调整只影响开发态导出格式，不改变 `/api/chat`、Agent runtime、模型实际输入或用户可见聊天协议。

## 2026-06-04 02:08:45 CST AiTrace 默认报告瘦身修复

为修正双文件导出第一版仍把完整 `rawTrace` / `trace` 结构留在默认报告里的问题，本次将 `ai_trace_log.js` 进一步收敛为 `traceSummary`、step summary、轻量 runtime event 摘要和模型调用摘要，不再内联完整 Raw trace 对象；`ai_trace_texts.jsonl` 同时按 hash 去重，重复 prompt 或 observation 只保存一份内容并记录出现路径。这样默认报告真正用于快速定位，长文本和重复内容按引用查找。

## 2026-06-04 02:21:11 CST AiTrace 模型请求长文本保真

为修复 30 多轮对话保存后长文本映射仍只有 800 字符摘要的问题，本次把 `DeepSeekModelAdapter` 的 request trace 从“长 message 直接摘要”调整为“长 message 分块诊断 envelope”。导出层会把 chunks 合并为单条 `contentRef` 长文本记录，默认报告继续只保留结构摘要和引用。这样常规排查仍省 token，需要复盘模型完整可见输入时可以从 `ai_trace_texts.jsonl` 查到尾部上下文。

## 2026-06-04 02:35:58 CST AiTrace 结构化详情外置

为修复默认报告瘦身后完整 `rawTrace` / `trace` 和 runtime event `input` / `output` / `metadata` 无法从 JSONL 找回的问题，本次把导出合同升级为 `contentRef` + `detailRef` 双引用。`ai_trace_log.js` 继续只保留轻量索引；`ai_trace_texts.jsonl` 同时保存长文本 chunks 和结构化详情 chunks，chunk 记录通过 `parentRef` 关联 header。这样两个文件组合后可以追溯完整脱敏诊断信息，而且按 header ref 查询时不会直接打印超长 chunk 内容。

## 2026-06-04 14:57:36 CST 统一可见训练方案输出合同

为让动作推荐、单次训练编排和长期计划共用同一条用户可见事实链路，本次新增 `final_answer.visibleOutputs[]` 通用 envelope，并把训练内容统一为 `visibleTrainingProposal` payload。`searchExerciseResources` 只保留只读候选查询职责，按 `suitabilities[]` 分组返回 `exerciseId`；最终方案事实只从已校验 `visible_output` 用户事件保存为 `visible_training_proposal_displayed`，下一轮通过 `readRecentVisibleTrainingProposal` 重新导入当前 run。旧 `exercise_recommendation_*` 事实桥主路径和旧 read tool 删除，不保留 alias 或兼容读取。

## 2026-06-04 15:25:52 CST 可见训练方案事实引用边界修复

为修正 `recentVisibleTrainingProposals` 同时扮演“历史事实索引”和“当前 run 动作来源”的边界混淆，本次收紧可见训练方案复用合同：metadata 只提供 `factRef` / `messageId`，不能直接支撑 `visibleTrainingProposal.exerciseItems[*].exerciseId`。历史动作必须先通过 `readRecentVisibleTrainingProposal` 导入为当前 run 的 `visible_training_proposal_fact` resource；read tool 在读数据库前会校验引用确实来自当前 run metadata，避免模型猜测旧 factRef 或绕过动作存在性复验。

## 2026-06-04 16:20:26 CST visibleTrainingProposal 引用查询合同扩展

为修复没有上一轮可见训练方案时模型仍把省略表达直接规划成动作刷新，并同步修正 `visibleOutputs[].schemaVersion` 数字提示问题，本次将旧 `readRecentVisibleTrainingProposal` delete-only 重命名为 `inspectVisibleTrainingProposals`。新 tool 通过 `operation = "list_recent"` 查询当前会话可引用事实索引，通过 `operation = "read_recent"` 导入完整可消费事实；`searchExerciseResources` 继续只查动作库，不承担历史事实判断。`schemaVersion` 模型可见合同统一为字符串 `"1"`，数字 `1` 只触发 repair，不做服务端兼容转换。

## 2026-06-04 18:37:15 CST 可见训练方案数据库事实校验解耦

为修复 `visibleTrainingProposal` 合法性依赖具体业务 `toolName` 的耦合，本次将最终动作校验改为异步 terminal output validator。生产装配层注入只读数据库事实服务，按最终 `exerciseItems[*].exerciseId` 批量校验动作存在、发布态和 `allowedSections`，并把有限 canonical 摘要传给 renderer。renderer 和历史 fact 读取不再从 `searchExerciseResources` / `inspectVisibleTrainingProposals` 的输出形态拼详情；历史方案再次复用前也会复核当前数据库状态。这样新增同类动作查询 tool 时不需要修改 terminal output core 或业务 toolName 白名单。

## 2026-06-04 19:05:43 CST 点名动作解析 Tool 与 requiredExerciseIds 查询合同

为修复多点名动作请求只进入单个 `searchExerciseResources(q)` 查询的问题，本次新增 `resolveExerciseResourceMentions` 只读 tool，由模型结构化传入 `mentions`，服务端只做发布态数据库动作事实解析。`searchExerciseResources` 同步支持 `requiredExerciseIds`，把已解析动作优先纳入现有 `groups.<section>.exercises`，无法纳入或筛选不一致时通过 `diagnostics` 说明原因。整个修复不新增服务端关键词拆词、自然语言模板路由或 `/api/chat` 分流，保持 Agent core 只负责合同和事实边界。

## 2026-06-04 19:38:59 CST 可见训练方案富卡片接入

为把统一后的 `visibleTrainingProposal` 用户可见事实接回成熟 UI，本次新增前端 adapter，将 `exercise_selection`、`routine`、`plan` 分别派生为 `ExerciseRecommendationCard`、`WorkoutRoutineDraftCard` 和 `WorkoutPlanDraftCard`。聊天页删除临时 `VisibleTrainingProposalPanel` 和旧 `bubble*` 训练卡片 fallback，聊天历史保存也不再写入旧 `plans` / `routines` / `exerciseRecommendations` / `recommendationIntents`。整个调整只发生在前端消费和持久化白名单，不修改 AI 输出合同、tool registry、事实桥或服务端语义分流。

## 2026-06-04 19:53:36 CST 可见训练方案富卡片补漏

为修复“推荐一批动作”已有 `visible_output` 但前端没有卡片的问题，本次补齐两个遗漏：旧推荐卡片 schema 上限扩展到 40，让 `visibleTrainingProposal.exerciseItems` 输出多少个动作就展示多少个动作，避免 12 个候选因 schema 校验失败后整卡消失；服务端聊天历史保存和恢复 `message.visibleOutputs`，并停止从旧 metadata 组装 `plans` / `routines` / `exerciseRecommendations` / `recommendationIntents`。这样刷新或回到历史会话时，训练卡片事实仍来自 `visibleOutputs`，旧 bubble 历史数据不再对外暴露。

## 2026-06-04 20:05:22 CST 可见训练方案卡片文案解耦

为修复训练富卡片头部文案和聊天正文重复的问题，本次断开 `message.content` 到 `visibleTrainingProposal` 富卡片 adapter 的输入链路。聊天正文继续独立渲染 Markdown，富卡片 `goal` / `summary` 只从 `visibleOutputs` 的结构化事实派生，例如动作数量、主要肌群、器械需求、训练日和休息日数量。这样卡片不再复制模型自然语言正文，保持“正文解释、卡片展示结构化训练结果”的边界。

## 2026-06-04 21:25:16 CST searchExerciseResources Facet Catalog 合同收敛

为修正 `bodyRegions` 让服务端承担高层身体区域语义展开的问题，本次将 `searchExerciseResources` 收敛为纯数据库 facet 查询：生产 registry 从发布态动作库读取完整 `facetCatalog` 并通过 manifest metadata 暴露给 Planner，tool 输入删除 `bodyRegions` / `expandedMuscles`，新增 `muscles` 多肌群 OR 查询。服务端只做 schema、去空去重和数据库 where 下推，不新增关键词、同义词或自然语言目标映射。

## 2026-06-04 21:45:48 CST Agent 可见 Plan 组合合同收紧

为修复多天训练目标只查 `training` 动作后降级成动作推荐的问题，本次把 `plan` 组合路径前移到模型首轮可见合同。Prompt 明确多天、周期、频次、训练日 / 休息日安排应优先使用 `payload.kind = "plan"`，并要求按 `training` 动作事实、`warmup` / `stretch` 补齐、`prescription` 和 `schedule.assignments` 的顺序组合。`searchExerciseResources` manifest 和 observation 同步说明该 tool 只提供动作事实原料，缺 section 时应继续查询或澄清，不新增服务端关键词分流或旧式 draft tool。

## 2026-06-04 22:04:34 CST Agent 活动条接入新核心链路

为恢复首页聊天请求处理中的可见推进感，本次在当前 `agent-core` 文本聊天主链新增 `agent_progress` 临时 UI 事件，并重建紧凑的 `AgentActivityIndicator`。旧 `agent_activity`、旧 `AgentOrchestrator`、旧 `assistant_action` 和旧卡片触发没有恢复；core 只提供非致命只读观察点，production chat adapter 负责安全阶段投影，前端活动状态只存在于当前请求内并在完成、失败、取消或会话切换时清理。

## 2026-06-04 22:50:12 CST 聊天 Hydration 与可见方案索引修复

为修复用户在 assistant 已回复后再次发送相同文本时，新 user turn 被服务端 hydration 去重误吞的问题，本次将 `prepareChatRequest()` 的去重条件收窄为“saved conversation 最后一条消息本身是同内容 user”才跳过追加；如果最后一条是 assistant，同文本也会作为新一轮输入进入模型上下文。同时将 `recentVisibleTrainingProposals` 的 run metadata 投影收敛为只含 `factRef` / `messageId`、版本、section 计数和 training 数量的索引摘要，完整训练方案必须继续通过 `inspectVisibleTrainingProposals(read_recent)` 导入当前 run，避免模型绕过正式 read/search 流程复用旧方案。

## 2026-06-04 23:54:15 CST 可见训练方案刷新合同收紧

为修复“重新来一套 / 换一批”后 Planner 可能按原始需求和同一排序重复生成上一套动作的问题，本次将刷新语义收口到模型实际可见合同：system prompt 明确 `visibleTrainingProposal` 刷新应保留原目标和约束，并优先替换上一套用户已看到 `exerciseItems`；`inspectVisibleTrainingProposals` 和 `searchExerciseResources` 的 manifest / observation 同步说明事实读取、`excludeExerciseIds` 来源和候选不足边界。服务端不新增自然语言短语分流，仍只做结构、权限、数据库事实和渲染前校验。

## 2026-06-05 12:18:38 CST Agent 修复方案抽象层级门禁 Skill

为防止后续 Agent 修复把单个 trace、用户原话、具体 `toolName`、字段组合或 phrasing 升格成通用生产规则，本次新增项目级 Codex Skill `agent-fix-abstraction-gate`。该 Skill 在开始实现 Agent / prompt / tool 修复或审查 OpenSpec change 前，要求先区分失败证据、通用合同、业务实例和回归测试；发现服务端关键词分流、短句模板规则、具体业务实例触发通用 prompt/runtime 等问题时必须暂停执行，并指出正确修复层级。该调整只影响协作治理流程，不修改 Agent runtime、生产 prompt 或 `/api/chat` 链路。

## 2026-06-05 12:47:52 CST Agent 引用连续性 Prompt 合同收紧

为修复省略表达拿到 `inspectVisibleTrainingProposals(list_recent)` 空索引 observation 后仍复读历史 assistant 能力介绍的问题，本次将通用 Agent prompt 升级到 `agent-action-v8`，补齐引用对象连续性规则：本轮请求、最近对话、metadata、observations 和 toolResults 共同支撑模型判断引用对象是否存在且可操作；历史 assistant 消息只能作为上下文参考，不再作为本轮回复模板。同时收紧 `list_recent` model projection 的 `facts[]` 事实边界，明确空索引只支持解释、澄清或推理，不能支撑成功刷新或新方案生成。服务端仍不新增自然语言短语分流、`factCount = 0` 分支或强制 tool result grounding guard。

## 2026-06-05 13:09:04 CST Agent 引用目标解析合同收紧

为修复引用型请求在缺少可操作对象时被模型降级成“重新生成一批动作”的问题，本次将通用 Agent prompt 明确为“引用型请求和独立生成请求是两个不同目标”：引用对象不可确认时，模型不能输出结构化结果并宣称完成替换、刷新或调整，只能解释缺少上下文、澄清或在用户提供独立目标时明确按新目标处理。`inspectVisibleTrainingProposals(list_recent)` 删除“开始新的生成”的宽松出口，`searchExerciseResources` observation 也明确动作查询不证明已有对象存在或刷新完成。整个调整仍不新增服务端关键词分流、具体 `toolName` 分支或 `facts=[]` 字段组合规则。

## 2026-06-05 13:46:49 CST 可见训练资源消费合同收紧

为修复已导入可见训练事实在“保留/复用”和“替换/排除”之间被模型混用，以及 `training` 动作事实被误当成完整 `routine` / `plan` 覆盖的问题，本次把资源消费边界升级为模型可见合同。默认 prompt 增加 `reuse` / `derive` / `modify` / `replace` / `clarify` 推理标签；`inspectVisibleTrainingProposals(read_recent)` 和 `searchExerciseResources` observation 明确 `availableSections`、`missingSectionsForRoutineOrPlan`、`supportsOutputKinds`、`requiredExerciseIds` 正向锚点与 `excludeExerciseIds` 负向排除边界；`visibleTrainingProposal` validator 也会在 section 缺失或 section 不合法时返回可恢复 coverage 诊断。服务端仍只校验结构、数据库事实和资源边界，不新增自然语言短句分流或固定 tool 流程。

## 2026-06-05 14:29:45 CST Agent Runtime 配置集中化

为解决模型请求参数、Agent run limits、tool 可见事实数量和 trace 裁剪常量分散在 adapter、聊天服务、tool、repository 和 fact store 中的问题，本次新增 `lib/server/config/` 作为服务端 Agent 配置入口。生产 prompt 配置迁移到同一目录，runtime config 按 `llm`、`runtime`、`tools`、`trace` 分组，并由 `DeepSeekModelAdapter`、`agent-text-chat-service` 和相关 tool 消费；repository / fact store 继续保留 hard cap。该调整不新增业务 tool、API 契约、环境变量预算 override 或服务端自然语言分流。

## 2026-06-05 15:12:35 CST Agent 活动阶段 Tool 定义同步

为降低聊天活动条和生产 tool 不同步的风险，本次删除 `agent-text-chat-service` 中具体业务 `toolName -> stage` 映射，把用户安全活动阶段改为 tool definition 上的内部 `uiActivityStage` 字段。生产聊天 adapter 只读取该字段、稳定 resource contract fallback 或通用 `analyzing_request`；前端继续用 `agentActivityDisplayByStage` 白名单展示中文短文案。`uiActivityStage` 不进入 Planner manifest，避免把 UI 展示字段暴露给模型。

## 2026-06-05 15:42:17 CST Agent Loop 轮次事件拆分

为修复聊天活动条把 `agent_progress` 数量误当成 Agent Loop 轮次的问题，本次新增独立 `agent_loop` stream 事件，由 `agent-core` runtime 真实 loop 边界产生，production chat adapter 只投影 `loopTurn` 和 `sequence`。前端活动状态拆成 `loopTurn` 与 `activityStage`，`#N` 只来自后端 loop 事件，Activity 仲裁、content 到达和重复 stage 都不再递增轮次。

## 2026-06-05 16:10:48 CST Agent 终态失败 Fallback 投影

为修复 Agent terminal validation / repair exhausted / timeout 等失败最终统一显示“聊天生成失败”的死路体验，本次在 production chat adapter 增加确定性失败分类投影。可分类失败会输出安全 `content`、`assistant_suggestions` 和 `done`，例如能力未接入、训练结构未通过校验、预算或超时；未分类错误仍走安全 `error`，但前端不再使用旧死路文案。`agent-core` 默认 `Response Renderer` 保持通用脱敏边界，trace 的 `response_write.output.projectionType` 记录具体投影类型，方便区分成功 `final_answer`、fallback 和 transport/config failure。

## 2026-06-05 17:01:30 CST routine / plan section readiness 合同硬化

为避免模型在已经看到 `missingSectionsForRoutineOrPlan` 后仍提交缺 section 的 `routine` / `plan`，本次把 `warmup`、`training`、`stretch` 三类事实覆盖写成 `final_answer.visibleOutputs[]` 的明确前置条件。默认 prompt、`searchExerciseResources` manifest / observation 和 validator recovery 同步说明缺口非空时禁止提交 `routine` / `plan` 结构，只能继续获取缺失 section、澄清或失败收口；服务端仍只做结构、数据库事实和资源边界校验，不新增训练生成 tool、关键词分流或固定 tool 调用流程。

## 2026-06-05 17:21:45 CST Agent 建议提问合同收敛

为解决聊天建议按钮字段在 `assistantSuggestions`、`suggestedReplies`、`assistant_suggestions`、`ask_user.suggestions` 和历史 `suggestedQuestions` 之间分裂的问题，本次把主链合同统一为 `suggestedQuestions?: string[]`。`final_answer` 和 `ask_user` 共用该可选字段，Response Renderer 输出 `suggested_questions`，前端按钮直接展示并发送同一文本，聊天历史只保存新字段；旧协议不迁移、不兼容，也不作为服务端语义分流入口。

## 2026-06-05 17:46:05 CST 基础 LLM 黑盒手动入口恢复

为修复合并后基础首页聊天黑盒 runner 被清理掉、`llm基础测试.md` 只剩人工用例的问题，本次恢复独立 `npm run test:llm:basic` 入口、manual Vitest config、runner、judge 和报告生成。默认 `npm run test` 继续只跑普通自动化测试，并移除默认测试目录中可通过 `RUN_DEEPSEEK_BLACKBOX=1` 触发真实模型调用的分支，确保 Codex 日常验证不会消费 token；真实首页聊天黑盒只能由手动命令执行。

## 2026-06-05 18:10:38 CST Agent 终态 Grounding 合同硬化

为修复工具已执行后 Planner 用无 grounding 成功 `final_answer` 承诺后续继续的问题，本次在 `Action Validator` 增加通用 terminal grounding guard：当前 run 已有 tool result 时，成功 `final_answer` 必须引用 satisfied tool result、consumable resource 或输出合法 `visibleOutputs[]`。默认 prompt 和两个业务 tool 的模型可见说明同步明确 `final_answer` 是本轮终态，不会在回复后自动继续 tool 调用；生产聊天 replay 覆盖原始失败类别和等价表达，确认被拒绝 content 不会渲染，repair 后仍可继续合法 tool 调用。

## 2026-06-05 18:27:29 CST 基础 LLM 黑盒误删恢复补全

为补齐 `add-llm-basic-chat-blackbox-tests` 被误删后的残留缺口，本次恢复该原始 OpenSpec change 的完整审阅文档、latest report 文件和基础 LLM 黑盒 fixture 不得泄漏进生产链路的架构扫描。实现代码不回滚到旧树，继续以当前恢复后的手动入口、当前报告时间格式和默认测试隔离边界为准。

## 2026-06-05 21:07:10 CST Agent 同义语义槽字段收敛

为降低模型在长 prompt、tool manifest 和 repair 循环中记错字段名的概率，本次把 Agent 终态文本统一为 `content`，terminal grounding 统一为 `usedRefs`，可见训练方案读取统一为 `ref: { type, value }`，动作肌群筛选统一为 `muscles` 数组。服务端没有新增自然语言关键词、正则或短句分流，仍只负责 schema、resource、tool result、权限和数据库事实边界校验。

## 2026-06-05 22:22:12 CST Agent Schema Repair Feedback 通用化

为避免 Agent repair loop 继续通过 runtime 动态文案解释旧字段、tool input 字段组合或 visible output 恢复方向，本次新增通用 `schema error projector`，把 `AgentAction` 和 tool input schema failure 投影为 `target`、`discriminator`、`errors[]`、`path`、`expected`、`actual`、`allowedFields`、`requiredFields` 和 `allowedValues`。terminal visible output 与 domain validator 失败改为 deterministic facts，删除模型可见 `repair`、`recoveryDirections`、`recoverableActions` 等下一步建议字段；默认 prompt 升级到 `agent-action-v15`，让模型按 schema / manifest / facts 自主修正下一轮 action，服务端仍不新增关键词、正则、短句模板或具体业务 `toolName` 分支。

## 2026-06-06 00:23:06 CST Agent ToolResult satisfied 边界移除

为修复 `ok=true` 但 `fulfillment.satisfied=false` 的 0 条结果、候选不足或诊断摘要被普通 `final_answer` grounding 拒绝的问题，本次把 Agent core 的普通文本收口边界改为只校验 current-run tool result 是否存在且 `ok=true`。`fulfillment.satisfied` 保留为诊断摘要，duplicate feedback 从 `duplicate_tool_success` 迁移为 `duplicate_tool_input`；结构化训练交付继续只由 `final_answer.visibleOutputs[]` 的 terminal validator 校验，正文不会被解析或保存成训练事实。trace 新增 `factChannel` 区分 fact、diagnostic 和 failed，方便复盘中间 tool fact 与最终 output validation 的层次。

## 2026-06-06 02:17:40 CST Agent 终态失败 Finalizer

为改善主 Agent repair / terminal validation 耗尽后的用户体验，本次在 production chat adapter 增加独立 terminal failure finalizer。主 Agent 仍按原合同失败收口，validator、ResourceStore、Policy Guard、Response Renderer 和可见训练方案事实保存边界不放宽；finalizer 只在 provider gate 允许时额外调用一次受限 LLM，输入为脱敏失败摘要，输出只能是普通 `content` 和可选 `suggestedQuestions`。trace 和手动 LLM 报告同步区分 `terminal_failure_finalizer`、确定性 fallback、provider unavailable 和主 Agent 成功，避免把失败解释误记为 `final_answer_success`。

## 2026-06-06 02:41:13 CST DeepSeek V4 Flash Thinking Mode 恢复

为修复 Agent Loop 重构后首页 `thinkingEnabled` 只停留在 metadata、没有进入 DeepSeek provider request 的缺口，本次把生产默认模型恢复为 `deepseek-v4-flash`，并在集中配置中声明 Thinking Mode 默认开启和 `reasoning_effort = "high"`。`DeepSeekModelAdapter` 只在 provider 边界映射 `thinking.type`，`reasoning_content` 只进入 trace 诊断，不参与 `AgentAction` 解析、不展示给用户、不写入聊天历史；terminal failure finalizer 继续作为低延迟失败解释模型调用，显式禁用 Thinking Mode。

## 2026-06-06 12:48:19 CST Agent 输出合同从 System Prompt 解耦

为避免默认 `system prompt` 继续承载 `visibleTrainingProposal`、section coverage、`prescription`、`schedule` 和业务 examples，本次新增集中 `outputContracts` 模型可见合同层。生产 Planner user payload 现在把 `outputContracts` 与 `tools`、`observations`、`toolResults` 并列发送，trace 只记录 output type 和 schema version 摘要；通用 prompt 收敛为 `AgentAction`、grounding、policy / resource / validator、不可执行能力和安全边界。terminal grounding 同步收紧为 failed、diagnostic、不可消费 resource 或 `satisfied=false` 结果不能支撑成功 `final_answer`，服务端仍不新增关键词、phrasing 或具体 `toolName` 语义分流。

## 2026-06-06 13:03:58 CST AgentAction 合同字段字典化

为解决默认 `system prompt` 仍像后端接口文档的问题，本次把 `AgentAction` 的最小形状、字段字典、决策顺序、grounding、repair 和 few-shot 拆到模型可见 `actionContract`，并与 `tools`、`outputContracts`、`observations`、`toolResults` 同级发送给 Planner。system prompt 进一步收敛为九条最小执行约束；`visibleTrainingProposal` output contract 补齐字段字典和高价值 examples，并明确当前 `plan = one routine template + schedule`，不支持 `routines[]` 或多模板训练日。服务端仍只做 schema、resource、grounding 和 validator 边界校验，不新增关键词或 phrasing 分流。

## 2026-06-06 13:25:35 CST Agent ToolManifest 瘦身分层

为解决生产 tool manifest 把工具说明、全局 Planner 禁令、资源生命周期和历史修复提示混在一起的问题，本次把通用 tool / resource / grounding glossary 收敛到 `actionContract`，并把 `ToolExample` 升级为完整 `tool_call` AgentAction。`inspectVisibleTrainingProposals`、`resolveExerciseResourceMentions` 和 `searchExerciseResources` 的 manifest 只保留各自事实能力、关键 input 和下游消费边界；`searchExerciseResources` 的 Planner 可见无器械输入值统一为 `equipment: "no_equipment"`。服务端不新增自然语言分流，也不修改 tool handler 或 Agent runtime 主循环。

## 2026-06-06 14:00:26 CST Terminal Failure Finalizer 输出校验放宽

为修复 finalizer 已返回可读失败解释和建议问题、却因 `content` 没命中特定中文短语而被丢弃的问题，本次把 terminal failure finalizer 输出校验收敛为 strict shape 校验：只要求 `content` 非空、`suggestedQuestions` 数量和长度受控、无额外字段。主 Agent 的训练方案、动作事实、resource、policy、renderer 和事实持久化校验不放宽；无效训练卡片仍不能展示或保存，失败解释只作为普通 `content` / `suggested_questions` 兜底返回。

## 2026-06-06 14:37:27 CST Agent Planner 输入分层

为避免模型把稳定协议、当前 run 事实和 repair 反馈混成同一优先级，本次将 Planner 模型输入拆成 `protocol`、`context` 和可选 `repairContext` 三层。`DeepSeekModelAdapter` 把 `AgentAction` 合同和 output contract 放进 system 协议层，user payload 只保留当前事实；validator 拒绝 action 后才追加独立 repair 语境。生产 tool observation 同步瘦身为事实等级、可消费边界、缺口字段、diagnostics 和 `nextActionHints`，不再输出固定 tool flow 或长篇下一步模板。服务端仍不新增关键词、phrasing 或具体业务 `toolName` 语义分流。

## 2026-06-06 14:52:36 CST Agent Planner 输入分层复查补强

为补齐输入分层实现中的剩余噪音，本次把 duplicate successful tool input 纳入独立 `repairContext`，并把 duplicate feedback 与 ok tool result index observation 的长文案恢复建议收敛成 `nextActionHints` 短枚举。`DeepSeekModelAdapter` 的 thinking 参数、trace 和去重统计改为读取 `context` 层，output contract 中所有 `expectedAction` 示例也改为可通过 `AgentActionSchema` 的完整对象。服务端仍不读取用户原文做关键词、phrasing 或业务 `toolName` 分流。

## 2026-06-06 16:40:54 CST Tool Observation 决策提示移除

为修复 tool observation 把确定性事实包装成业务决策提示的问题，本次移除生产模型可见 observation 和 duplicate `repairContext` 中的 `supportsOutputKinds`、`supportsSuccessfulVisibleOutputs`、`finalAnswerSupport`、`nextActionHints` 和 `routinePlanCompositionBoundary`。`searchExerciseResources`、`inspectVisibleTrainingProposals`、`resolveExerciseResourceMentions` 和 ok tool result index 现在只暴露事实等级、引用边界、section coverage、查询事实、导入事实、diagnostics 和可恢复边界；共享 coverage helper 收敛为 `sectionSummary`、`availableSections` 和 `missingSections`。Planner 继续基于用户目标、tool facts、resource 和 output contract 自主选择 `tool_call`、`final_answer` 或 `ask_user`，服务端不新增关键词、phrasing、自然语言模板路由或具体业务 `toolName` 语义分支。

## 2026-06-06 16:52:30 CST visibleTrainingProposal plan kind 合同硬化

为修复正文承诺每周训练计划但结构化输出仍退化为无 `schedule` 的单次 `routine`，本次收紧 `visibleTrainingProposal` output contract：`routine` 只表示一次可执行训练编排，`plan` 表示 `one routine template + schedule`，正文中的频次、周期、多天或一周安排必须由同一 payload 的 `kind = "plan"` 和 `schedule.assignments` 支撑。测试覆盖 production Planner 请求、chat service 计划事件、架构边界和手动 LLM judge，确认修复留在模型可见合同层，没有新增服务端关键词、phrasing 或 toolName 语义分流。

## 2026-06-06 17:45:16 CST searchExerciseResources support section 查询策略收紧

为修复热身 / 拉伸动作复用主训练 strict hard filters 后容易被 `level`、`q`、分类或标签查空的问题，本次给 `searchExerciseResources` 增加 section-aware hard filter policy。`training` 继续严格下推全部结构化筛选，`warmup` / `stretch` 只把发布态、section、器械、场地、肌群和受控动作 id 作为 hard filter；未应用字段通过 `filterApplications` 结构化披露给模型、用户投影和 trace summary。服务端没有新增用户原文关键词、phrasing、自然语言模板路由、RAG 或 Agent core 里的业务 toolName 分支。

## 2026-06-06 17:57:08 CST 基础 LLM 黑盒冒烟口径调整

为让 `npm run test:llm:basic` 只承担基础链路可用性验证，本次移除基础套件的独立 judge 模型判分，把通过条件收敛为 `/api/chat` 响应可解析、收到 `done`、会话保存成功且存在用户可见回答。大模型正文、terminal failure finalizer 输出、服务端确定性兜底、安全错误文案、建议提问和确认请求都可作为基础冒烟通过依据；`llm基础测试.md` 的期望列继续进入报告供人工复核，语义质量问题后续放到 detailed suite 或专项回归处理。

## 2026-06-07 12:05:52 CST Agent activitySummary 受控投影

为让聊天活动条展示模型本轮准备推进的用户目标，本次给三类 `AgentAction` 新增可选 `activitySummary`。模型只在受控 action JSON 中提供短中文摘要，服务端只做 schema 校验、安全 sanitizer、trace 投影和当前请求内 `agent_progress` 输出，前端只做二次校验和临时展示。`agent_loop` 仍是唯一 loop 轮次来源；摘要不进入 `ChatMessage`、聊天历史、conversation summary、visible output、artifact payload、跨 run fact bridge 或 replay summary，也不参与 tool 选择、权限、grounding、最终回答或结构化输出决策。

## 2026-06-07 14:43:36 CST AI Usage 后台汇总

为把生产 token 统计从诊断 Trace 中解耦出来，本次新增 `AiTokenUsageSummary` 生产汇总表和 `/admin` 只读后台。聊天链路会把 planner、repair 和 terminal failure finalizer 的可获取 usage 合并为同一请求 / assistant 消息级汇总，写入失败不阻断用户响应；后台通过集中配置的管理员 userId 和统一 guard 授权，只消费 admin service 投影，不读取 Trace、导出日志或 `ChatMessage.metadata`。partial usage 和 provider 未返回 usage 会显式标记未知，避免被误读为真实 0。
