## 1. 语音提示模型

- [x] 1.1 新增训练语音提示工具，将 `WorkoutTimelineStep` 数据转换为中文的第一个动作、准备倒计时、动作、休息、下一组动作和按次计数提示文案。
- [x] 1.2 新增简洁的训练概览生成逻辑，在保留总动作数量的同时限制过长动作列表。
- [x] 1.3 为提示文案生成补充聚焦测试，覆盖计时步骤、按次步骤、按次计数、组间休息、动作切换、循环间隙和长训练。

## 2. 浏览器音频 Hook

- [x] 2.1 实现仅客户端使用的 `useWorkoutVoiceBroadcast` hook，封装 `window.speechSynthesis` 和 `SpeechSynthesisUtterance`。
- [x] 2.2 实现语音队列取消逻辑，覆盖暂停、继续、跳步、完成、关闭播报和组件卸载流程。
- [x] 2.3 为计时模式动作的每个已训练秒数实现短促本地 beep 提示，并在浏览器音频不可用时优雅降级。
- [x] 2.4 为按次模式动作实现数字计数语音提示，并去重，确保同一步骤中每个达到的计数只播报一次。
- [x] 2.5 优先使用 `zh-CN` 语音设置和可用中文 voice，不可用时回退到浏览器默认 voice。

## 3. 训练页面接入

- [x] 3.1 在 `WorkoutSessionPage` 中用语音播报开关替换现有顶部音频按钮行为，同时保留当前视觉提示。
- [x] 3.2 默认开启语音播报，除非 `localStorage` 中存在关闭偏好。
- [x] 3.3 使用 `fitmate.*` localStorage key 持久化开关变化，不增加服务端持久化。
- [x] 3.4 将 hook 接入训练 timeline 状态，使启动序列、步骤切换、手动跳步、暂停、继续和训练完成触发正确音频行为。
- [x] 3.5 保持 `/plans`、`/composer`、AI routes、训练持久化 API 和训练数据 schema 不变。

## 4. 验证

- [x] 4.1 运行与改动文件相关的 TypeScript 和 lint 检查。
- [x] 4.2 验证不支持 `speechSynthesis`、`localStorage` 不可用、audio context 不可用时不会破坏训练页面。
- [ ] 4.3 手动验证 `/training` 默认开启语音，音量按钮可切换并持久化本地偏好，每个动作步骤开始前先播报动作名和目标量，等待动作提示播报完成后再播报“3，2，1，开始”，倒计时结束后才开始该动作计时，休息步骤不触发动作准备倒计时，计时模式动作每秒 beep，按次模式动作按重复间隔播报“1，2，3...”。
