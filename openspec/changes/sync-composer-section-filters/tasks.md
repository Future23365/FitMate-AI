## 1. 用途适配派生与接口契约

- [ ] 1.1 梳理并集中动作 suitability 派生规则，支持 `warmup`、`training`、`stretch` 非互斥结果。
- [ ] 1.2 为 suitability 派生补充单元测试，覆盖适合热身、适合主训练、适合拉伸、同一动作适合多个用途和高风险动作不轻易进入热身。
- [ ] 1.3 将 `/api/exercises` 的右侧动作库用途筛选参数定义为 `suitability=warmup|training|stretch`，确认 `all` 不进入接口契约。
- [ ] 1.4 梳理旧 `workoutSection` 查询参数使用点，确保新 UI 不再依赖它表达动作库用途筛选。

## 2. Facets 作用域同步

- [ ] 2.1 调整动作库 facets 生成逻辑，使分类、肌群、器械、难度、居家条件选项可以按当前 `suitability` 范围收集。
- [ ] 2.2 调整 `/api/exercises` 返回逻辑，使列表结果叠加所有筛选条件，但 facets 只受当前用途范围影响。
- [ ] 2.3 补充接口或服务测试，验证选择适合热身、适合主训练、适合拉伸时返回的 facets 不包含该用途范围外的值。

## 3. 动作编排页筛选 UI

- [ ] 3.1 将右侧动作库顶部筛选状态改为 `all | warmup | training | stretch`，默认选中“全部”。
- [ ] 3.2 在顶部入口中渲染“全部、适合热身、适合主训练、适合拉伸”，选择“全部”时请求不携带 `suitability`。
- [ ] 3.3 根据当前用途范围返回的 facets 渲染下方分类、肌群、器械、难度、居家条件选项。
- [ ] 3.4 用途切换后校验已有下方筛选值，只清除在新用途范围内不可用的筛选，保留仍然有效的筛选。
- [ ] 3.5 保持右侧用途筛选不改变中间编排区当前添加阶段，添加动作仍使用当前选中的编排阶段。
- [ ] 3.6 确认“清空筛选”只清空搜索词和下方辅助筛选，不改变当前用途入口。

## 4. 验证

- [ ] 4.1 验证“全部”请求不携带 `suitability`，适合热身、适合主训练、适合拉伸请求分别携带对应用途值。
- [ ] 4.2 验证用途切换后下方筛选项与当前用途同步，不出现已知冲突选择。
- [ ] 4.3 验证同一动作可以出现在多个适合用途筛选结果中。
- [ ] 4.4 验证从右侧动作库添加动作时，动作落点仍由中间编排区当前添加阶段决定。
- [ ] 4.5 运行 `npm test` 或与动作库筛选相关的测试。
- [ ] 4.6 运行 `npm run typecheck`。
