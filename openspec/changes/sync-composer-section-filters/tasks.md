## 1. 阶段推断与接口契约

- [ ] 1.1 梳理并集中动作阶段推断规则，确保拉伸优先、热身其次、训练兜底。
- [ ] 1.2 为阶段推断补充单元测试，覆盖中文关键词、英文关键词、拉伸优先和训练兜底。
- [ ] 1.3 保持 `/api/exercises` 的 `workoutSection` 只接受 `warmup`、`training`、`stretch`，确认 `all` 不进入接口契约。

## 2. Facets 作用域同步

- [ ] 2.1 调整动作库 facets 生成逻辑，使分类、肌群、器械、难度、居家条件选项可以按当前 `workoutSection` 范围收集。
- [ ] 2.2 调整 `/api/exercises` 返回逻辑，使列表结果叠加所有筛选条件，但 facets 只受当前阶段范围影响。
- [ ] 2.3 补充接口或服务测试，验证选择热身、训练、拉伸时返回的 facets 不包含该阶段范围外的值。

## 3. 动作编排页筛选 UI

- [ ] 3.1 将右侧动作库阶段筛选状态改为 `all | warmup | training | stretch`，默认选中“全部”。
- [ ] 3.2 在阶段入口中渲染“全部、热身、训练、拉伸”，选择“全部”时请求不携带 `workoutSection`。
- [ ] 3.3 根据当前阶段范围返回的 facets 渲染下方分类、肌群、器械、难度、居家条件选项。
- [ ] 3.4 阶段切换后校验已有下方筛选值，只清除在新阶段范围内不可用的筛选，保留仍然有效的筛选。
- [ ] 3.5 保持右侧阶段筛选不改变中间编排区当前添加阶段，添加动作仍使用当前选中的编排阶段。
- [ ] 3.6 确认“清空筛选”只清空搜索词和下方辅助筛选，不改变当前阶段入口。

## 4. 验证

- [ ] 4.1 验证“全部”请求不携带 `workoutSection`，热身、训练、拉伸请求分别携带对应阶段值。
- [ ] 4.2 验证阶段切换后下方筛选项与当前阶段同步，不出现已知冲突选择。
- [ ] 4.3 验证从右侧动作库添加动作时，动作落点仍由中间编排区当前添加阶段决定。
- [ ] 4.4 运行 `npm test` 或与动作库筛选相关的测试。
- [ ] 4.5 运行 `npm run typecheck`。
