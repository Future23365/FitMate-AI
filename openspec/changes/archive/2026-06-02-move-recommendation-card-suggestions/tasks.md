## 1. 前端展示调整

- [x] 1.1 移除推荐卡片组件内固定的“换一批”和“编成训练”按钮接口与渲染。
- [x] 1.2 让推荐卡片底部接收并展示同条消息的 `assistantSuggestions`，点击后发送建议 `message`。
- [x] 1.3 聊天页在消息存在推荐卡片时，将建议从正文下方移动到卡片底部；非推荐消息保留原展示位置。
- [x] 1.4 删除仅服务旧固定按钮的前端状态和处理函数，避免无入口逻辑残留。

## 2. 服务端建议投影

- [x] 2.1 调整 Agent Response Writer，只投影 Agent 显式返回的 `replyContext.assistantSuggestions`。
- [x] 2.2 移除推荐工具结果触发的默认“生成训练/换一批”建议注入。

## 3. 验证

- [x] 3.1 更新 Agent Response Writer 测试，覆盖显式建议保留和无显式建议时不注入默认按钮。
- [x] 3.2 运行相关自动化测试。
- [x] 3.3 运行 TypeScript 类型检查。
