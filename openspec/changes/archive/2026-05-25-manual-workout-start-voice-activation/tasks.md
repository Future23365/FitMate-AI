## 1. 训练开始状态

- [x] 1.1 在 `WorkoutSessionPage` 新增显式 `hasStarted` 状态，并在计划加载、完成训练和必要的重置路径中恢复为待开始状态。
- [x] 1.2 用 `hasStarted` 保护准备倒计时、主计时、自动计次、beep 和自动推进，确保进入页面不会自动开始训练。
- [x] 1.3 调整待开始状态下的步骤切换行为：可查看或选择步骤，但不自动启动倒计时。

## 2. 开始按钮与训练控制

- [x] 2.1 新增主“开始”按钮，待开始时替代暂停/继续语义，点击后从当前步骤开始训练。
- [x] 2.2 开始后恢复现有暂停、继续、上一个、下一个、跳过休息和结束训练行为。
- [x] 2.3 调整页面文案和状态标签，区分待开始、准备中、进行中、暂停和已完成。

## 3. 语音激活规则

- [x] 3.1 移除任意 `pointerdown` / `keydown` 训练控制自动恢复语音的逻辑。
- [x] 3.2 在开始按钮点击流中，当语音偏好开启且浏览器支持时尝试激活当前步骤语音。
- [x] 3.3 保留语音按钮的开启、关闭和重试能力，但移除“刷新后需要点击一次恢复播报”类提示。
- [x] 3.4 确保暂停、继续、跳步、列表选择不会触发恢复语音提示或隐式激活语音。
- [x] 3.5 开始按钮只做静默音频解锁和当前动作播报，不播报“语音播报已开启”。
- [x] 3.6 修复 speech fallback 误判：无 `onstart` / `onend` 时标记 `speech_blocked`，不再把 beep 当成语音成功。
- [x] 3.7 支持首次 voices 列表为空时等待 `voiceschanged` 或短超时后再创建 speech utterance。

## 4. 待开始界面

- [x] 4.1 删除待开始状态中间的大播放图标，保留底部主“开始”按钮作为唯一醒目的开始入口。

## 5. 测试与验证

- [x] 5.1 补充或调整单元测试，覆盖进入页面未开始、点击开始后才推进、开始按钮激活语音、暂停不触发恢复语音。
- [x] 5.2 运行 `npm test` 或相关测试，覆盖训练页语音和步骤流。
- [x] 5.3 运行 `npm run typecheck`。
- [x] 5.4 运行 `npm run lint`。
- [x] 5.5 使用 Chrome DevTools MCP 复用已有 `http://localhost:3000/training` 验证页面进入不自动开始、点击开始后才训练、暂停不出现恢复语音提示。
