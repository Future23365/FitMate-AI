## 1. 时间线顺序与列表状态

- [x] 1.1 梳理 `workout-session-page.tsx` 中所有基于动作 `id`、`findIndex()` 或展开列表索引推断当前步骤的位置。
- [x] 1.2 基于 `buildWorkoutTimeline()` 输出派生训练列表 view model，包含列表项顺序、`itemIndex`、所属 section、循环轮次、完成状态、当前状态和跳转目标 step index。
- [x] 1.3 将当前动作、下一个动作、列表高亮和列表点击跳转统一改为使用时间线顺序与 view model，不再用重复动作 `id` 推断轮次位置。
- [x] 1.4 确保自动计时结束、顶部“下一个”和训练控制区跳步都复用同一个步骤切换入口，并按新步骤类型重置剩余时长、准备倒计时、示范图和语音状态。

## 2. 循环列表与休息跳过 UI

- [x] 2.1 在训练项目列表中为多轮主训练动作展示循环轮次标识，并保持单轮训练时的列表简洁。
- [x] 2.2 调整休息步骤的主操作或控制区，当前步骤为 `rest` 时显示明确的“跳过休息”按钮。
- [x] 2.3 实现“跳过休息”进入当前休息后的下一个时间线步骤，并清理旧步骤倒计时、语音和节奏提示回调。
- [x] 2.4 保持非休息动作步骤的暂停、继续、上一个、下一个和结束训练能力不变。

## 3. 语音提示 UI

- [x] 3.1 调整首次语音开启提示，使其比当前小气泡更醒目，并与顶部音量按钮或训练控制区形成清晰视觉关联。
- [x] 3.2 为刷新后等待激活和播放失败状态提供更明显的提示与可操作入口，避免只依赖图标颜色或弱提示文字。
- [x] 3.3 确保语音提示不会遮挡主要计时、当前动作和关键控制按钮，并符合现有浅色 MD3 风格。

## 4. 验证

- [x] 4.1 为训练时间线或训练列表派生逻辑补充测试，覆盖多轮循环、重复动作 `id`、组间休息、动作间休息和循环间隙。
- [x] 4.2 运行与改动相关的自动化检查，至少包含 `npm test` 或相关测试命令，以及 `npm run typecheck`。
- [x] 4.3 使用 Chrome DevTools MCP 验证 `/training`：多轮循环列表轮次可区分，“下一个”按顺序推进，休息步骤可跳过休息，语音提示醒目且 Console/Network 无异常。
- [x] 4.4 复查 OpenSpec 变更状态，确保 proposal、design、specs 和 tasks 均可用于 apply 阶段。
