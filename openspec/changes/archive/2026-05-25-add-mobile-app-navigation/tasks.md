## 1. 侧栏结构复用

- [x] 1.1 将 `AppSidebar` 的导航、新建对话、历史对话和删除逻辑整理为可复用内部面板组件
- [x] 1.2 保持桌面端固定侧边栏在 `lg` 以上的现有布局、视觉和路由排除规则

## 2. 移动端导航

- [x] 2.1 在 `lg` 以下增加移动端导航入口，并确保 `/dev`、`/training` 不显示该入口
- [x] 2.2 实现移动端导航抽屉、遮罩、关闭按钮和背景滚动锁定
- [x] 2.3 确保导航项、新建对话、历史对话点击后执行原有操作并关闭抽屉

## 3. 验证

- [x] 3.1 运行相关 TypeScript 或 lint 检查
- [x] 3.2 使用 OpenSpec 校验确认 change/spec 文档有效
