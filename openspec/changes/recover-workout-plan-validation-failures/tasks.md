## 1. 服务端恢复模型

- [ ] 1.1 增加校验问题分类工具，区分可恢复问题和硬边界问题。
- [ ] 1.2 扩展 `/api/ai/workout-plan` 的失败响应结构，返回 `recoverable`、`guidanceMessage`、`suggestedReplies` 和校验详情。
- [ ] 1.3 在 AI Trace 中记录首次校验失败、自动修复请求、修复输出、二次校验结果和最终响应。

## 2. 自动修复流程

- [ ] 2.1 为训练草稿生成服务增加一次性修复请求，输入原始草稿、校验错误、用户 intent 和候选动作。
- [ ] 2.2 针对 `session_too_long` 明确要求 LLM 压缩到目标时长附近，并减少动作数量、组数、循环轮数或休息配置。
- [ ] 2.3 修复后的草稿必须重新走同一套 Schema、动作候选和训练规则校验。
- [ ] 2.4 修复通过时返回修复后的有效草稿，修复失败时返回可恢复引导。

## 3. 前端引导体验

- [ ] 3.1 更新 `requestWorkoutPlanDraft` 的错误解析，保留可恢复失败的结构化信息。
- [ ] 3.2 更新 `use-chat-controller` 的 silent generation 错误处理，展示“计划生成失败”和可继续对话的引导文案。
- [ ] 3.3 为可恢复失败提供建议回复，例如“压缩到 30 分钟”“保留完整训练量”“减少动作数量”“降低每个动作组数”。
- [ ] 3.4 确认未通过校验的草稿不会展示为可保存训练卡片。

## 4. 测试与验证

- [ ] 4.1 补充服务端测试，覆盖 `session_too_long` 自动修复通过。
- [ ] 4.2 补充服务端测试，覆盖自动修复仍失败时返回可恢复引导。
- [ ] 4.3 补充客户端或 Hook 测试，覆盖可恢复失败不会抛成普通终止错误。
- [ ] 4.4 运行 `npm test`、`npm run typecheck` 和 `npm run lint`；如影响构建边界，再运行 `npm run build` 或说明无法运行原因。

## 5. 文档记录

- [ ] 5.1 在 `docs/方案变更历史` 新增变更记录，说明校验失败从终止型错误改为自动修复和对话引导。
