## 1. 布局基线与影响盘点

- [x] 1.1 盘点 `app/globals.css` 中当前断点配置，以及项目内 `md:`、`lg:`、`xl:`、`2xl:` 在主应用 shell、动作编排页、训练计划页和训练执行页的使用点。
- [x] 1.2 设计并落地 PC-only 桌面断点基线，确保普通桌面、宽桌面和超宽桌面能表达不同工作区布局。
- [x] 1.3 提取或整理工作区布局宽度 token，覆盖左侧展开宽度、左侧收缩宽度、右侧辅助面板宽度和主内容最小宽度。

## 2. 全局左侧导航收缩态

- [x] 2.1 为 `AppSidebar` 增加展开态和收缩态状态模型，保持当前路由选中态稳定。
- [x] 2.2 实现收缩态 UI：保留核心导航图标、新建入口、设置入口和当前选中反馈，隐藏历史列表、导航文字和用户名称。
- [x] 2.3 将主内容左侧留白改为跟随 sidebar 展开/收缩态变化，避免页面各自硬编码固定 `pl-[260px]`。
- [x] 2.4 确认 `/dev` 与 `/training` 仍按既有规则不挂载主应用 sidebar，不被全局 sidebar 状态影响。

## 3. 公共右侧辅助面板

- [x] 3.1 基于现有 `shared-right-drawer` 能力扩展或新增工作区辅助面板外壳，复用 portal、遮罩、滚动锁定、ESC 关闭和 `drawer-open` 联动。
- [x] 3.2 支持调用方配置面板标题、宽度、内容区样式和内部 tab/二级视图，同时保持公共外壳不依赖训练领域类型。
- [x] 3.3 为工作区页面提供常驻停靠与按需浮出的切换接口，避免业务页重复实现右侧面板外壳。

## 4. 动作编排页迁移

- [x] 4.1 将 `/composer` 的固定右侧动作库/已保存编排栏迁移为可停靠素材面板。
- [x] 4.2 在普通桌面宽度下提供明确的“动作库 / 已保存编排”打开入口，并让浮出面板关闭后释放主编排区宽度。
- [x] 4.3 保持动作库搜索、筛选、动作详情、添加动作能力和已保存编排打开、复制、删除能力不变。
- [x] 4.4 确保浮出动作库中添加动作后面板保持打开，支持连续添加动作。
- [x] 4.5 调整动作编排行布局，保证普通桌面宽度下动作名称、阶段、组数、目标和休息设置可读可操作。

## 5. 训练计划页迁移

- [x] 5.1 将 `/plans` 的固定右侧当天计划栏迁移为可停靠日期详情面板。
- [x] 5.2 在普通桌面宽度下提供当前选中日期详情入口，并通过右侧浮出面板展示当天完整安排。
- [x] 5.3 保持从已保存编排添加、安排训练、设置休息日、更新状态、删除安排和开始训练能力不变。
- [x] 5.4 调整月历主体宽度和溢出策略，确保普通桌面宽度下七列日期结构可扫读。
- [x] 5.5 收敛本月统计、最近动态和快速建议等次级信息，避免它们在普通桌面宽度下长期挤压月历主体。

## 6. 回归与验证

- [x] 6.1 静态检查 `/training` 布局，确认全局断点调整没有破坏训练执行页的一屏训练体验。
- [x] 6.2 运行 `npm run lint`。
- [x] 6.3 运行 `npm run typecheck`。
- [x] 6.4 运行 `npm test`。
- [x] 6.5 运行 `npm run build`，或记录无法运行的具体原因。
- [x] 6.6 运行 `openspec validate responsive-workout-workspace-layout --strict`。
