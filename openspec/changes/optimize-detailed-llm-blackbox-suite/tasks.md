## 1. Fixture 去重与元数据

- [x] 1.1 为 `BlackboxFlowCase` 增加 suite/group/riskLevel 等运行元数据，并为现有基础与详细 flow 补齐元数据。
- [x] 1.2 建立详细套件重复矩阵，至少覆盖严格重复序列、重复起点和高重叠 setup。
- [x] 1.3 合并或改写 `F03` / `C06` 的重复覆盖，确保“不重复追问”类独有语义断言仍被保留。
- [x] 1.4 合并或改写 `W08` / `M03` 的重复覆盖，确保局部 patch 与后续降级链路仍被覆盖。
- [x] 1.5 梳理 `W05` / `C03`、`F13` / `H03`、`P03` / `P04` / `M05` 等高重叠 flow，明确保留原因或调整输入。
- [x] 1.6 增加不调用真实模型的 fixture 元数据测试，校验每个 flow 有 suite/group，且没有未标记的严格重复序列。

## 2. 子集筛选与失败重跑

- [x] 2.1 重构 flow 获取逻辑，让 runner 可以基于 suite、group、flow id 和失败报告生成待运行 flow 列表。
- [x] 2.2 为 `scripts/run-manual-llm-tests.mjs` 增加 flow id 筛选参数，并在未知 id 或空结果时阻止真实模型调用。
- [x] 2.3 增加 group 筛选参数，并在未知 group 时输出可用 group 列表。
- [x] 2.4 增加 suite 层级筛选参数，支持基础、详细核心、详细扩展等明确层级。
- [x] 2.5 增加从最近报告重跑 failed flow 的参数，解析失败 flow id 并只运行失败 flow。
- [x] 2.6 增加不调用真实模型的筛选策略测试，覆盖 id、group、suite、失败报告、空结果和未知参数。

## 3. 受控并发执行

- [x] 3.1 为 runner 增加显式并发配置，默认并发数保持 1。
- [x] 3.2 实现 flow 级队列并发，确保同一 flow 内三轮对话仍按顺序串行执行。
- [x] 3.3 确保并发结果按 fixture 定义顺序写入报告，而不是按完成时间写入。
- [x] 3.4 在报告和控制台摘要中记录并发配置。
- [x] 3.5 增加不调用真实模型的并发排序测试，验证汇总数、失败跳过和报告顺序稳定。

## 4. 动态规模、token 预估与报告

- [x] 4.1 移除 `scripts/run-manual-llm-tests.mjs` 中基础/详细套件 flow 数和 turn 数硬编码，改为从 fixture 与筛选结果动态计算。
- [x] 4.2 调整 token 预估逻辑，使预估基于本次实际运行集合，而不是完整详细套件固定轮次数。
- [x] 4.3 更新缺 key 跳过报告，展示完整 fixture 数、本次筛选数、跳过原因和筛选条件。
- [x] 4.4 更新真实运行报告，增加运行范围摘要、筛选条件、未运行原因、并发配置和去重检查摘要。
- [x] 4.5 增加不调用真实模型的报告生成测试，覆盖完整运行、子集运行、空筛选、缺 key 跳过和 failed-from-report。

## 5. 文档维护

- [x] 5.1 更新 `docs/manual-llm-consistency-tests.md`，说明基础套件、详细核心、详细扩展、按 id/group/suite 运行和失败重跑方式。
- [x] 5.2 在 `docs/方案变更历史` 新增一份本次详细 LLM 黑盒套件去重与提速方案记录，时间使用上海时间精确到秒。
- [x] 5.3 如实现改变长期测试体系边界，在 `docs/项目演变历程.md` 追加摘要记录。

## 6. 验证

- [x] 6.1 运行与 fixture 元数据、筛选策略、报告生成和并发排序相关的不调用真实模型测试。
- [x] 6.2 运行 `npm run typecheck`。
- [x] 6.3 运行 `openspec validate optimize-detailed-llm-blackbox-suite --strict`。
- [x] 6.4 在缺少或不使用 `DEEPSEEK_API_KEY` 的情况下验证子集筛选和跳过报告不会调用真实模型。
- [x] 6.5 仅在用户明确确认 token 成本后，运行真实 `npm run test:llm` 或指定的详细子集命令刷新报告；本次未获 token 成本确认，未运行真实模型。
