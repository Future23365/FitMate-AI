## 1. Voice 列表排序

- [x] 1.1 增加稳定的 voice 排序逻辑，按 `zh-CN`、其他中文、非中文分组展示。
- [x] 1.2 确认排序不改变已选 voice 的保存、回显和训练播报配置。

## 2. 弹窗动效

- [x] 2.1 为语音设置弹窗增加进入 / 关闭动效。
- [x] 2.2 弹窗打开时让主训练页面出现向后缩的视觉效果，并在关闭后恢复。
- [x] 2.3 检查动效不影响训练计时、暂停状态、当前步骤和语音开关状态。

## 3. 自检失败建议

- [x] 3.1 扩展自检结果或 UI 判断，识别 API 存在但自检失败、阻止或超时的状态。
- [x] 3.2 在上述状态下展示“可尝试重启浏览器后再检测”的恢复建议，并保留具体失败步骤。

## 4. 浏览器 API 版本展示

- [x] 4.1 为 Chrome、Edge、Firefox、Safari、iOS Safari 增加本地彩色图标展示。
- [x] 4.2 调整 API 版本区域布局，确保图标、浏览器名称和版本信息在桌面与移动宽度下不重叠。

## 5. 验证

- [x] 5.1 运行 `openspec validate polish-workout-voice-settings-dialog --strict`。
- [x] 5.2 运行 `npm run typecheck`、`npm run lint` 和相关测试。
- [x] 5.3 使用 Chrome DevTools MCP 验证 `/training` 弹窗排序、动效、自检失败提示和浏览器图标展示；如果本地服务不可访问，记录原因。

## 6. 追加视觉打磨

- [x] 6.1 将语音设置弹窗的页面缩放、遮罩模糊和动画节奏对齐动作详情抽屉。
- [x] 6.2 使用本地真实彩色 SVG 浏览器图标替换字母伪图标，并验证布局一致性。
- [x] 6.3 修正弹窗主体打开动画，并让背景缩放保持到关闭动画结束。
